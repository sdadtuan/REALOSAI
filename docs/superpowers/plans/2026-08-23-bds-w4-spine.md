# W4 — Spine v1: hook ticket idempotent (U-12)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replay hold / TX / lead / launch không nhân ticket — cùng `idempotency_key` trả về ticket cũ (U-12). Nền cho W5–W8, không bus mới.

**Architecture:** Nest-first, reuse-first. Spine v1 = helper bọc `StaffTicketService.createHandoffTicket` đã có. Unique index P12 (`uq_crm_staff_tickets_open_entity`, `uq_crm_staff_tickets_idem`) **đã tồn tại** — W4 bắt race 23505 + ghi `idempotency_key` (hiện hook **không** set). Không Kafka, không `bds_spine_events`, không HTTP spine mới.

**Tech Stack:** NestJS + Jest (`ptt-crm-api`). Ops-web **không** đổi trừ khi import type lệch (không kỳ vọng).

**Spec:** [2026-08-23-bds-role-feature-execution.md](../specs/2026-08-23-bds-role-feature-execution.md) §6–7 W4  
**Unification:** [2026-08-23-bds-crm-os-unification-design.md](../specs/2026-08-23-bds-crm-os-unification-design.md) §4.3, §12.3, U-12  
**OS plan:** [2026-08-23-bds-os-coding.md](./2026-08-23-bds-os-coding.md) Task 13  
**W3 (xong):** [2026-08-23-bds-w3-commission-ui.md](./2026-08-23-bds-w3-commission-ui.md)

## Global Constraints

- Không thay Q1–Q48. Không rewrite `BdsHoldService` / `BdsTxService` / `BdsCollectionService` — chỉ đổi **khối gọi ticket** sang helper.
- Không `Bds2Module`, không `/crm/bds-v2`, không board CSKH (W5), không ẩn B2B (W6), không finance/CAPI (W7), không offboard (W8).
- **Cấm** Kafka, **cấm** bảng `bds_spine_events`, **cấm** job `bds_spine_dispatch`, **cấm** `GET /api/v1/bds/spine/buyer/:leadId` (W5).
- Không ghi HH vào `crm_b2b_commission_ledger`. Không payroll (U7).
- Flag: không tắt `PTT_BDS_PACK` / `PTT_STAFF_TICKETS` trên staging. Helper no-op khi `!isStaffTicketsEnabled()` hoặc `tickets` null — giữ `@Optional()`.
- Test API: `cd services/ptt-crm-api && ./node_modules/.bin/jest <file> --runInBand`
- Không commit trừ khi user yêu cầu.
- `idempotency_key` spine = `event_type + ':' + aggregate_id + ':' + stage` (unification §12.3). Trùng → cùng ticket (mọi status, không chỉ `open`).
- Unique PG đã có: `uq_crm_staff_tickets_idem` `(tenant_id, idempotency_key)` WHERE key NOT NULL; `uq_crm_staff_tickets_open_entity` WHERE status IN (`open`,`in_progress`). Không ADD index mới trừ U-12 FAIL trên VPS (escalate).

### Catalog hook đã sống → key W4

Chỉ bọc chỗ **đã** gọi `createHandoffTicket`. Không invent queue mới.

| `event_type` | `stage` | Queue | Aggregate | Call site |
|--------------|---------|-------|-----------|-----------|
| `buyer.created` | `created` | `cskh_first_touch` | lead id | `bds-buyer-lead.service.ts` |
| `hold.created` | `pending` | `hold_f1_approve` | hold id | `bds-hold.service.ts` |
| `tx.deposit` | `deposit` | `collection_schedule` | tx id | `bds-tx.service.ts` convert-deposit |
| `tx.vbtt` | `vbtt` | `vbtt_check` | tx id | `bds-tx.service.ts` vbtt |
| `tx.contracted` | `contracted` | `commission_period` | tx id | `bds-tx.service.ts` contract |
| `tx.hdmb_gate` | `legal` / `paid` | `hdmb_gate_legal` / `hdmb_gate_paid` | tx id | `maybeCreateHdmbGateTickets` |
| `legal.gate` | `so_xd` | `legal_gate_phase` | project id | `bds-project-os.service.ts` |
| `launch.opened` | `open` | `ops_action` | launch id | `bds-launch.service.ts` |

Ví dụ: hold `h-abc` → `hold.created:h-abc:pending`.

### Gap hôm nay (khóa — đúng code)

- `createHandoffTicket` chỉ `getOpenByEntity` (status `open`/`in_progress`). Ticket **done** + hook chạy lại → **insert ticket thứ hai**. Đó là lỗ U-12.
- `createHandoffTicket` **không** ghi `idempotency_key` dù cột + unique index đã có (`createTicket` HTTP thì có).
- Race hai insert song song: unique 23505 **chưa** bắt → lỗi nuốt ở `catch` logger, hoặc nổ.
- Hold HTTP idem (`bds_idempotency_keys`) đã replay body **trước** hook — lần 2 cùng header không gọi ticket. U-12 vẫn cần khi hook được gọi lại (retry sau done, hoặc hai process).

### Ngoài W4 (cấm trong PR này)

- Board `flow=re_buyer`, Deal Room hide trên 360, after auto row (W5).
- Nav ẩn `/crm/sales` (W6). Finance hub / CAPI Purchase (W7). Offboard hold (W8).
- Emit `domain_events` platform (đã có service riêng) — YAGNI W4.
- Đổi contract hold/TX/receipt (W1). Đổi page commissions (W3).

---

## File map

```
services/ptt-crm-api/src/bds/spine/bds-spine-idempotency.ts          CREATE — key + catalog
services/ptt-crm-api/src/bds/spine/bds-spine-idempotency.spec.ts     CREATE
services/ptt-crm-api/src/bds/spine/bds-existing-hook-replay.ts       CREATE — bọc createHandoffTicket
services/ptt-crm-api/src/bds/spine/bds-existing-hook-replay.spec.ts  CREATE
services/ptt-crm-api/src/staff-tickets/staff-ticket.service.ts       NÂNG — key + 23505
services/ptt-crm-api/src/staff-tickets/staff-ticket.service.spec.ts  NÂNG
services/ptt-crm-api/src/staff-tickets/staff-ticket.types.ts         NÂNG input optional key (nếu cần)
services/ptt-crm-api/src/bds/hold/bds-hold.service.ts                NÂNG chỉ khối ticket
services/ptt-crm-api/src/bds/hold/bds-hold.service.spec.ts           NÂNG U-12 hold
services/ptt-crm-api/src/bds/buyers/bds-buyer-lead.service.ts        NÂNG chỉ khối ticket
services/ptt-crm-api/src/bds/transactions/bds-tx.service.ts          NÂNG chỉ khối ticket
services/ptt-crm-api/src/bds/transactions/bds-tx.service.spec.ts     NÂNG (nếu thiếu assert key)
services/ptt-crm-api/src/bds/project-os/bds-project-os.service.ts    NÂNG chỉ khối ticket
services/ptt-crm-api/src/bds/launches/bds-launch.service.ts          NÂNG chỉ khối ticket
```

API **GIỮ** (không thêm route Nest):

| Việc | Đã có |
|------|--------|
| Tạo ticket handoff | `StaffTicketService.createHandoffTicket` |
| Unique open entity | `uq_crm_staff_tickets_open_entity` |
| Unique idem | `uq_crm_staff_tickets_idem` + `getByIdempotencyKey` |
| Hold/TX HTTP replay | `bds_idempotency_keys` |

---

### Task 1: Key spine + catalog

**Files:**
- Create: `services/ptt-crm-api/src/bds/spine/bds-spine-idempotency.ts`
- Create: `services/ptt-crm-api/src/bds/spine/bds-spine-idempotency.spec.ts`

**Interfaces:**
- Consumes: không
- Produces: `bdsSpineIdempotencyKey({ event_type, aggregate_id, stage })`; `BDS_SPINE_HANDOFF` hằng map (để Task 3–4 không typo queue)

- [ ] **Step 1: Failing test**

```ts
import { BDS_SPINE_HANDOFF, bdsSpineIdempotencyKey } from './bds-spine-idempotency';

describe('bdsSpineIdempotencyKey', () => {
  it('joins event_type:aggregate_id:stage', () => {
    expect(
      bdsSpineIdempotencyKey({
        event_type: 'hold.created',
        aggregate_id: 'h-abc',
        stage: 'pending',
      }),
    ).toBe('hold.created:h-abc:pending');
  });

  it('defaults stage to default', () => {
    expect(
      bdsSpineIdempotencyKey({ event_type: 'buyer.created', aggregate_id: '9' }),
    ).toBe('buyer.created:9:default');
  });
});

describe('BDS_SPINE_HANDOFF', () => {
  it('maps hold.created to hold_f1_approve', () => {
    expect(BDS_SPINE_HANDOFF['hold.created'].queue_code).toBe('hold_f1_approve');
    expect(BDS_SPINE_HANDOFF['buyer.created'].queue_code).toBe('cskh_first_touch');
    expect(BDS_SPINE_HANDOFF['tx.deposit'].queue_code).toBe('collection_schedule');
  });
});
```

- [ ] **Step 2: Run — expect FAIL**

Run: `cd services/ptt-crm-api && ./node_modules/.bin/jest src/bds/spine/bds-spine-idempotency.spec.ts --runInBand`  
Expected: FAIL — module chưa có.

- [ ] **Step 3: Implement**

```ts
export type BdsSpineEventType =
  | 'buyer.created'
  | 'hold.created'
  | 'tx.deposit'
  | 'tx.vbtt'
  | 'tx.contracted'
  | 'tx.hdmb_gate'
  | 'legal.gate'
  | 'launch.opened';

export function bdsSpineIdempotencyKey(input: {
  event_type: string;
  aggregate_id: string;
  stage?: string;
}): string {
  const eventType = String(input.event_type ?? '').trim();
  const aggregateId = String(input.aggregate_id ?? '').trim();
  const stage = String(input.stage ?? 'default').trim() || 'default';
  return `${eventType}:${aggregateId}:${stage}`;
}

export const BDS_SPINE_HANDOFF: Record<
  BdsSpineEventType,
  { queue_code: string; entity_type: string; default_stage: string }
> = {
  'buyer.created': { queue_code: 'cskh_first_touch', entity_type: 'lead', default_stage: 'created' },
  'hold.created': { queue_code: 'hold_f1_approve', entity_type: 'hold', default_stage: 'pending' },
  'tx.deposit': { queue_code: 'collection_schedule', entity_type: 'tx', default_stage: 'deposit' },
  'tx.vbtt': { queue_code: 'vbtt_check', entity_type: 'tx', default_stage: 'vbtt' },
  'tx.contracted': { queue_code: 'commission_period', entity_type: 'tx', default_stage: 'contracted' },
  'tx.hdmb_gate': { queue_code: 'hdmb_gate_legal', entity_type: 'tx', default_stage: 'legal' },
  'legal.gate': { queue_code: 'legal_gate_phase', entity_type: 'project', default_stage: 'so_xd' },
  'launch.opened': { queue_code: 'ops_action', entity_type: 'launch', default_stage: 'open' },
};
```

`tx.hdmb_gate` mặc định `legal` — Task 4 truyền `stage: 'paid'` khi queue `hdmb_gate_paid`.

- [ ] **Step 4: Run — expect PASS**

Cùng lệnh Step 2. Expected: PASS.

- [ ] **Step 5:** Không commit trừ khi user yêu cầu.

---

### Task 2: Harden `createHandoffTicket`

**Files:**
- Modify: `services/ptt-crm-api/src/staff-tickets/staff-ticket.service.ts` (`createHandoffTicket` + `maybeCreateHdmbGateTickets`)
- Modify: `services/ptt-crm-api/src/staff-tickets/staff-ticket.service.spec.ts`

**Interfaces:**
- Consumes: `repo.getByIdempotencyKey`, `repo.getOpenByEntity`, `repo.insertTicket` (đã có)
- Produces: `createHandoffTicket(..., { idempotency_key?: string })` — lookup key **mọi status**; insert kèm key; 23505 → return existing

- [ ] **Step 1: Failing tests** (thêm vào spec hiện có)

```ts
  it('createHandoffTicket replays by idempotency_key even when done', async () => {
    repo.getByIdempotencyKey.mockResolvedValue({
      id: 'tk-old',
      status: 'done',
      queue_code: 'hold_f1_approve',
    });
    const out = await svc.createHandoffTicket('t1', {
      queue_code: 'hold_f1_approve',
      title: 'Duyệt',
      body: '',
      entity_type: 'hold',
      entity_id: 'h1',
      idempotency_key: 'hold.created:h1:pending',
    });
    expect(out?.id).toBe('tk-old');
    expect(repo.insertTicket).not.toHaveBeenCalled();
  });

  it('createHandoffTicket stores idempotency_key on insert', async () => {
    repo.getByIdempotencyKey.mockResolvedValue(null);
    repo.getOpenByEntity.mockResolvedValue(null);
    repo.getQueue.mockResolvedValue({
      code: 'hold_f1_approve',
      sla_minutes: 120,
      assignee_dept_code: 'ban_kd',
    });
    repo.nextNumber.mockResolvedValue('ST-1');
    repo.insertTicket.mockResolvedValue({ id: 'tk-new' });
    await svc.createHandoffTicket('t1', {
      queue_code: 'hold_f1_approve',
      title: 'Duyệt',
      body: '',
      entity_type: 'hold',
      entity_id: 'h1',
      idempotency_key: 'hold.created:h1:pending',
    });
    expect(repo.insertTicket).toHaveBeenCalledWith(
      expect.objectContaining({ idempotency_key: 'hold.created:h1:pending' }),
    );
  });

  it('createHandoffTicket 23505 returns existing by key', async () => {
    repo.getByIdempotencyKey
      .mockResolvedValueOnce(null)
      .mockResolvedValueOnce({ id: 'tk-race', queue_code: 'hold_f1_approve' });
    repo.getOpenByEntity.mockResolvedValue(null);
    repo.getQueue.mockResolvedValue({
      code: 'hold_f1_approve',
      sla_minutes: 120,
      assignee_dept_code: 'ban_kd',
    });
    repo.nextNumber.mockResolvedValue('ST-2');
    repo.insertTicket.mockRejectedValue({ code: '23505' });
    const out = await svc.createHandoffTicket('t1', {
      queue_code: 'hold_f1_approve',
      title: 'Duyệt',
      body: '',
      entity_type: 'hold',
      entity_id: 'h1',
      idempotency_key: 'hold.created:h1:pending',
    });
    expect(out?.id).toBe('tk-race');
  });
```

Giữ test cũ `createHandoffTicket is idempotent on open entity+queue`.

- [ ] **Step 2: Run — expect FAIL**

Run: `cd services/ptt-crm-api && ./node_modules/.bin/jest src/staff-tickets/staff-ticket.service.spec.ts --runInBand`  
Expected: FAIL — chưa nhận `idempotency_key` / chưa bắt 23505.

- [ ] **Step 3: Implement**

Trong `createHandoffTicket`, **trước** `getOpenByEntity`:

```ts
const idemKey = String(input.idempotency_key ?? '').trim();
if (idemKey) {
  const byKey = await this.repo.getByIdempotencyKey(tid, idemKey);
  if (byKey) return byKey;
}
```

`insertTicket` thêm `idempotency_key: idemKey || null`.

Bọc `insertTicket` trong try/catch: nếu `err.code === '23505'` (hoặc `err?.driverError?.code`) → `getByIdempotencyKey` rồi `getOpenByEntity`; có thì return; không thì rethrow.

`maybeCreateHdmbGateTickets` — truyền key:

```ts
idempotency_key: bdsSpineIdempotencyKey({
  event_type: 'tx.hdmb_gate',
  aggregate_id: tx.id,
  stage: queueCode === 'hdmb_gate_paid' ? 'paid' : 'legal',
}),
```

Import helper từ `../bds/spine/bds-spine-idempotency` — **không** đảo dependency: `staff-tickets` đã được `BdsModule` import; helper **thuần string**, không import `StaffTicketService`. Được.

Nếu reviewer e ngại tickets → bds: đặt `bdsSpineIdempotencyKey` gọi inline trong `maybeCreateHdmbGateTickets` bằng cùng công thức (copy 1 dòng) **hoặc** move helper sang `staff-tickets/spine-key.ts`. Ưu tiên **một** file `bds/spine/bds-spine-idempotency.ts`; `maybeCreateHdmbGateTickets` import helper thuần.

- [ ] **Step 4: Run — expect PASS**

Cùng lệnh Step 2. Expected: PASS (kể cả BDS-55/56 cũ).

- [ ] **Step 5:** Không commit trừ khi user yêu cầu.

---

### Task 3: Helper replay + hold / buyer

**Files:**
- Create: `services/ptt-crm-api/src/bds/spine/bds-existing-hook-replay.ts`
- Create: `services/ptt-crm-api/src/bds/spine/bds-existing-hook-replay.spec.ts`
- Modify: `services/ptt-crm-api/src/bds/hold/bds-hold.service.ts` (chỉ khối `hold_f1_approve`)
- Modify: `services/ptt-crm-api/src/bds/hold/bds-hold.service.spec.ts`
- Modify: `services/ptt-crm-api/src/bds/buyers/bds-buyer-lead.service.ts` (chỉ khối `cskh_first_touch`)

**Interfaces:**
- Consumes: `StaffTicketService.createHandoffTicket`, `bdsSpineIdempotencyKey`, `BDS_SPINE_HANDOFF`
- Produces: `replayHandoffTicket(tickets, tenantId, { event_type, aggregate_id, stage?, title, body, requester_dept_code?, project_id? })`

- [ ] **Step 1: Failing tests**

```ts
// bds-existing-hook-replay.spec.ts
import { replayHandoffTicket } from './bds-existing-hook-replay';

describe('replayHandoffTicket', () => {
  it('calls createHandoffTicket with spine key', async () => {
    const tickets = { createHandoffTicket: jest.fn().mockResolvedValue({ id: 'tk1' }) };
    await replayHandoffTicket(tickets as never, 't1', {
      event_type: 'hold.created',
      aggregate_id: 'h1',
      title: 'Duyệt hold',
      body: 'SP 9',
      requester_dept_code: 'ban_kenh',
      project_id: 7,
    });
    expect(tickets.createHandoffTicket).toHaveBeenCalledWith('t1', {
      queue_code: 'hold_f1_approve',
      title: 'Duyệt hold',
      body: 'SP 9',
      entity_type: 'hold',
      entity_id: 'h1',
      requester_dept_code: 'ban_kenh',
      project_id: 7,
      idempotency_key: 'hold.created:h1:pending',
    });
  });

  it('no-ops when tickets is null', async () => {
    await expect(
      replayHandoffTicket(null, 't1', {
        event_type: 'hold.created',
        aggregate_id: 'h1',
        title: 'x',
        body: '',
      }),
    ).resolves.toBeNull();
  });
});
```

Hold spec — thêm (mock `tickets` trên constructor nếu chưa có):

```ts
  it('U-12: second hold create with same idempotency does not enqueue a second ticket', async () => {
    const tickets = { createHandoffTicket: jest.fn().mockResolvedValue({ id: 'tk1' }) };
    // wiring: svc dùng cùng tickets mock; lần 1 insert hold; lần 2 getIdempotency replay
    repo.getIdempotency.mockResolvedValueOnce(null).mockResolvedValueOnce({
      created_at: new Date(),
      response_json: { id: 'h1', status: 'pending', tenant_id: 't1', project_id: 9, product_id: 1 },
    });
    // ... lần 1 create thành công gọi tickets 1 lần
    // lần 2 cùng key — expect tickets.createHandoffTicket).toHaveBeenCalledTimes(1)
  });
```

Nếu hold spec hiện **không** inject `tickets`, thêm `@Optional` mock như `bds-tx.service.spec.ts` — **không** đổi logic approve/409.

- [ ] **Step 2: Run — expect FAIL**

Run: `cd services/ptt-crm-api && ./node_modules/.bin/jest src/bds/spine/bds-existing-hook-replay.spec.ts src/bds/hold/bds-hold.service.spec.ts --runInBand`  
Expected: FAIL — helper chưa có / hold chưa assert ticket.

- [ ] **Step 3: Implement helper**

```ts
import type { StaffTicketService } from '../../staff-tickets/staff-ticket.service';
import {
  BDS_SPINE_HANDOFF,
  type BdsSpineEventType,
  bdsSpineIdempotencyKey,
} from './bds-spine-idempotency';

export async function replayHandoffTicket(
  tickets: Pick<StaffTicketService, 'createHandoffTicket'> | null | undefined,
  tenantId: string,
  input: {
    event_type: BdsSpineEventType;
    aggregate_id: string;
    stage?: string;
    title: string;
    body: string;
    requester_dept_code?: string | null;
    project_id?: number | null;
    queue_code?: string;
    entity_type?: string;
  },
): Promise<{ id: string } | null> {
  if (!tickets) return null;
  const catalog = BDS_SPINE_HANDOFF[input.event_type];
  const stage = input.stage ?? catalog.default_stage;
  const out = await tickets.createHandoffTicket(tenantId, {
    queue_code: input.queue_code ?? catalog.queue_code,
    title: input.title,
    body: input.body,
    entity_type: input.entity_type ?? catalog.entity_type,
    entity_id: String(input.aggregate_id),
    requester_dept_code: input.requester_dept_code ?? null,
    project_id: input.project_id ?? null,
    idempotency_key: bdsSpineIdempotencyKey({
      event_type: input.event_type,
      aggregate_id: String(input.aggregate_id),
      stage,
    }),
  });
  return out;
}
```

Hold — thay khối `this.tickets?.createHandoffTicket(...)` bằng:

```ts
await replayHandoffTicket(this.tickets, String(hold.tenant_id ?? unitTenant), {
  event_type: 'hold.created',
  aggregate_id: hold.id,
  title: `Duyệt hold ${String(hold.id).slice(0, 8)}`,
  body: `Hold chờ duyệt · SP ${productId}`,
  requester_dept_code: 'ban_kenh',
  project_id: hold.project_id,
});
```

Giữ `try/catch` + `isStaffTicketsEnabled()` như cũ.

Buyer — `event_type: 'buyer.created'`, `aggregate_id: String(lead.id)`, title/body giữ nguyên, `requester_dept_code: 'ban_mkt'`.

- [ ] **Step 4: Run — expect PASS**

Cùng lệnh Step 2. Expected: PASS. Hold 409 / idem HTTP cũ không regress.

- [ ] **Step 5:** Không commit trừ khi user yêu cầu.

---

### Task 4: Wire TX / legal / launch

**Files:**
- Modify: `services/ptt-crm-api/src/bds/transactions/bds-tx.service.ts` (3 khối ticket)
- Modify: `services/ptt-crm-api/src/bds/transactions/bds-tx.service.spec.ts`
- Modify: `services/ptt-crm-api/src/bds/project-os/bds-project-os.service.ts`
- Modify: `services/ptt-crm-api/src/bds/launches/bds-launch.service.ts`

**Interfaces:**
- Consumes: `replayHandoffTicket`
- Produces: cùng queue như catalog; `tx.hdmb_gate` vẫn qua `maybeCreateHdmbGateTickets` (Task 2 đã gắn key)

- [ ] **Step 1:** Test TX deposit đã gọi `createHandoffTicket` — **nâng** expect `idempotency_key: 'tx.deposit:{txId}:deposit'` nếu spec inspect args. Nếu spec chỉ `toHaveBeenCalledWith(tenant, expect.objectContaining({ queue_code: 'collection_schedule' }))` thì thêm `idempotency_key`.

- [ ] **Step 2: Replace 3 khối TX**

Deposit:

```ts
await replayHandoffTicket(this.tickets, String(tx.tenant_id ?? opts.tenantId ?? ''), {
  event_type: 'tx.deposit',
  aggregate_id: tx.id,
  title: `Cọc TX ${String(tx.id).slice(0, 8)} — lập lịch 4h`,
  body: `Cọc TX ${tx.id}`,
  requester_dept_code: 'ban_kd',
});
```

VBTT: `event_type: 'tx.vbtt'`, title/body giữ.  
Contract `createHandoffTicket` `commission_period`: `event_type: 'tx.contracted'`. **Giữ** `systemCloseEntityQueues` — không bọc, không đổi.

Legal: `event_type: 'legal.gate'`, `aggregate_id: String(projectId)`.  
Launch: `event_type: 'launch.opened'`, `aggregate_id: updated.id`.

Không đụng `enqueueOnConflict` launch queue (FIFO căn) — **không** phải staff ticket.

- [ ] **Step 3: Run**

```bash
cd services/ptt-crm-api && ./node_modules/.bin/jest \
  src/bds/transactions/bds-tx.service.spec.ts \
  src/bds/launches/bds-launch.service.spec.ts \
  src/staff-tickets/staff-ticket.service.spec.ts \
  --runInBand
```

Expected: PASS.

- [ ] **Step 4:** Không commit trừ khi user yêu cầu.

---

### Task 5: Kiểm chứng U-12 + không lộ W5+

**Files:** không thêm feature.

- [ ] **Step 1: Full related jest**

```bash
cd services/ptt-crm-api && ./node_modules/.bin/jest \
  src/bds/spine \
  src/staff-tickets/staff-ticket.service.spec.ts \
  src/bds/hold/bds-hold.service.spec.ts \
  src/bds/transactions/bds-tx.service.spec.ts \
  src/bds/launches/bds-launch.service.spec.ts \
  --runInBand
```

Expected: xanh. Không regress 409 hold / cổng HĐMB.

- [ ] **Step 2: Grep PR**

Không `Bds2Module`, không `bds_spine_events`, không `Kafka`, không `bds-spine-dispatch`, không `spine/buyer`, không sửa `commissions/page.tsx`, không `shouldHideDealRoom` trên `leads/[id]` (W5), không `PTT_BDS_NAV_HIDE_B2B`.

- [ ] **Step 3:** FE vitest không bắt buộc. Nếu đụng ops-web: `cd services/ops-web && ./node_modules/.bin/vitest run src/lib/bds` — kỳ vọng 62/62 (W3).

- [ ] **Step 4:** Không commit trừ khi user yêu cầu.

**UAT staging (sau deploy API — W4 có Nest):**

| Việc | Pass |
|------|------|
| Hold F1 2 lần cùng `idempotency-key` | 1 hold, 1 ticket `hold_f1_approve` |
| Ingest / tạo lead `re_buyer` 2 lần (cùng lead) | 1 ticket `cskh_first_touch` |
| Convert-deposit 2 lần cùng key | 1 TX, 1 ticket `collection_schedule` |
| Ticket F1 đã `done` + hook replay (nếu repro) | không ticket mới cùng key |
| VPS: không bảng `bds_spine_events` | `\d bds_spine_events` fail |

Deploy W4 **khác W3:** cần restart `realosai-api` (Nest). Không DDL mới nếu unique P12 đã apply.

---

## Coverage vs spec §7 W4 / U-12

| Tiêu chí | Task |
|----------|------|
| Event hold/tx replay, idempotent | 3, 4 |
| U-12 trùng key không nhân ticket | 2, 3 |
| Key `event_type:aggregate:stage` | 1 |
| Không bus / không bảng spine | 5 |
| Nền W5–W8 | helper sẵn — W5 không invent enqueue thứ hai |

## Escalate (chỉ khi U-12 FAIL trên PG)

| Gap | Hướng |
|-----|--------|
| Unique index P12 thiếu trên VPS | `APPLY` lại `docs/specs/postgresql-ddl-bds-p12.sql` — **không** invent bảng |
| 23505 không map từ pg driver | Bắt `err.code` **và** message `duplicate key` — ghi rõ trong PR |
| Vẫn nhân ticket sau unique + helper | **lúc đó** mới xét `bds_spine_events` — ngoài W4 mặc định |

---

*Sau khi duyệt plan: cập nhật link Task 13 trong [2026-08-23-bds-os-coding.md](./2026-08-23-bds-os-coding.md) và hàng W4 trong [2026-08-23-bds-role-execution.md](./2026-08-23-bds-role-execution.md).*
