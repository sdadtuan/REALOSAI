# P3 Triển khai — CSBH + bảng giá

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** CSBH trên PG: CV giá soạn `draft`; GĐKD (`cdt_sales_dir`) activate gắn đợt + snapshot `price_list_id` (UC-009); chiết khấu vượt cap không duyệt → 400 `{ error: 'discount_cap' }` (BDS-12). Helper `one_price` sẵn cho P4/P5.

**Architecture:** Bounded context `src/bds/policies/` (`BdsPolicyService` — spec §5). HTTP `/api/v1/bds` sau `StaffOrInternalKeyGuard` + `BdsPackGuard` + `BdsPolicyGuard` (`PTT_BDS_PACK=1` **và** `PTT_BDS_POLICY=1`). Tenant stamp từ dự án, không từ body. Không import `ReProjectsModule`. Không TX, không agency, không UI.

**Tech Stack:** NestJS `ptt-crm-api`, Jest local binary, `pg` Pool, `psql`.

**Spec:** [2026-08-21-bds-industry-pack-design.md](../specs/2026-08-21-bds-industry-pack-design.md) §6.3, §10.4, §15 P3, BR-BDS-26.  
**UC:** 009 soạn + activate CSBH.  
**P1b:** [2026-08-22-bds-p1b-project-os.md](./2026-08-22-bds-p1b-project-os.md)  
**P2:** [2026-08-22-bds-p2-hold-ttl.md](./2026-08-22-bds-p2-hold-ttl.md)  
**Roadmap:** [2026-08-22-bds-coding-roadmap.md](./2026-08-22-bds-coding-roadmap.md)

**Test ID P3:** BDS-12.  
**BDS-33 / BR-BDS-26 HTTP trên TX đại lý** = **P4/P5** — P3 chỉ ship util `assertOnePrice` + gọi trong `quote` khi client gửi `net_price_vnd`.  
**BDS-11** `deposit_min` = **P4**. Cột lưu, không enforce.

## Global Constraints

- `PTT_BDS_PACK` mặc định `0` — mọi `POST /api/v1/bds/*` = **404**.
- `PTT_BDS_POLICY` mặc định `0` — route CSBH = **404** dù PACK=1. An toàn prod (giống HOLD_TTL / PROJECT_OS).
- GET ngoài tenant = 404, không PII (BR-BDS-05). Optional `x-bds-tenant` giống inventory.
- Không xóa `re-projects/`. Không đụng `crm_b2b_projects`. Không tạo `bds_transactions` / `bds_agencies`.
- Một dự án **một** CSBH `active` theo `audience` (partial unique). Activate mới → archive cái cũ cùng audience.
- Activate chỉ khi `actor_role === 'cdt_sales_dir'` — 403 `{ error: 'activate_forbidden' }` (UC-009 E1). JWT RBAC đầy đủ = **P8**.
- `discount_pct > discount_cap_pct` và `discount_approved !== true` → 400 `{ error: 'discount_cap' }` (BDS-12).
- `one_price` đã có trên `crm_re_projects` (P0, mặc định true). P3 không đổi cột đó.
- Không dual-write bảng giá SQLite. API giá P3 chỉ PG. `re-projects` price-list SQLite giữ nguyên khi PACK=0.
- `BdsModule` **không** import `ReProjectsModule`.
- Folder `policies/` (roadmap) — **không** đụng `src/policy/` (engine nền).
- `DATABASE_URL` script: `postgresql://ptt:ptt_dev@127.0.0.1:5433/rnosaidb`.
- Test: `cd services/ptt-crm-api && ./node_modules/.bin/jest <file> --runInBand` (không `npx jest`).
- Không commit trừ khi user yêu cầu.
- Prod VPS: **không** bật `PTT_BDS_PACK` / `PTT_BDS_POLICY`. Không đụng `ngoinhahomnay.vn` / :3000.

---

## 0. Phạm vi / ngoài phạm vi

**Làm**

- Flag `isBdsPolicyEnabled()` + `BdsPolicyGuard`
- DDL `bds_sales_policies`; PG stub `crm_re_price_lists` / `_items` (+ `tenant_id`, `policy_id`); cột snapshot trên `bds_launch_phases`
- Draft CRUD CSBH; draft bảng giá + items
- Activate: role GĐKD, archive sibling, gắn `phase.policy_id` + `phase.price_list_id`
- Archive (để activate CSBH mới cùng audience)
- `POST /policies/:id/quote` — BDS-12 + one_price helper khi có `net_price_vnd`
- Util VAT/PBT (tính, không sinh lịch TT)

**Không làm**

- Convert cọc / `deposit_min` 400 (P4, BDS-11)
- TX / phiếu thu / installment từ `payment_template_json` (P4 / P4b)
- Agency gửi net ≠ CSBH trên TX (P5, BDS-33) — util sẵn
- Apply giá lên `crm_re_project_products` hàng loạt (SQLite `apply` cũ)
- Wire `hold_ttl_minutes` vào `BdsHoldService` (P2 đã có tenant settings)
- Launch khóa giá / TTL 180s (P10)
- UI `/crm/bds` tab Giá/CSBH (P8)
- Cap `bds_policies` trên JWT (P8)

---

## 1. File map

```
docs/specs/postgresql-ddl-bds-p3.sql
scripts/apply_pg_ddl_bds_p3.sh

services/ptt-crm-api/src/bds/bds.flags.ts                         # + isBdsPolicyEnabled
services/ptt-crm-api/src/bds/bds.flags.spec.ts
services/ptt-crm-api/src/config/app-config.service.ts             # bdsPolicyEnabled
services/ptt-crm-api/src/bds/guards/bds-policy.guard.ts
services/ptt-crm-api/src/bds/guards/bds-policy.guard.spec.ts
services/ptt-crm-api/src/bds/policies/bds-policy.types.ts
services/ptt-crm-api/src/bds/policies/bds-policy.util.ts
services/ptt-crm-api/src/bds/policies/bds-policy.util.spec.ts
services/ptt-crm-api/src/bds/policies/bds-policy.repository.ts
services/ptt-crm-api/src/bds/policies/bds-policy.service.ts
services/ptt-crm-api/src/bds/policies/bds-policy.service.spec.ts
services/ptt-crm-api/src/bds/policies/bds-policy.controller.ts
services/ptt-crm-api/src/bds/policies/bds-policy.controller.spec.ts
services/ptt-crm-api/src/bds/bds.module.ts
docs/superpowers/plans/2026-08-22-bds-coding-roadmap.md           # hàng P3
```

---

### Task 1: Flag POLICY + util CSBH

**Files:**
- Modify: `services/ptt-crm-api/src/bds/bds.flags.ts`
- Modify: `services/ptt-crm-api/src/bds/bds.flags.spec.ts`
- Modify: `services/ptt-crm-api/src/config/app-config.service.ts` — `bdsPolicyEnabled` cạnh `bdsHoldTtlEnabled`
- Create: `services/ptt-crm-api/src/bds/guards/bds-policy.guard.ts`
- Create: `services/ptt-crm-api/src/bds/guards/bds-policy.guard.spec.ts`
- Create: `services/ptt-crm-api/src/bds/policies/bds-policy.types.ts`
- Create: `services/ptt-crm-api/src/bds/policies/bds-policy.util.ts`
- Create: `services/ptt-crm-api/src/bds/policies/bds-policy.util.spec.ts`

**Interfaces:**
- Produces: `isBdsPolicyEnabled(): boolean`
- Produces: `BdsPolicyGuard` → 404 unless PACK **và** POLICY
- Produces: `canActivatePolicy(actorRole: string): boolean` — chỉ `cdt_sales_dir`
- Produces: `assertDiscountAllowed(capPct, requestedPct, approved): void` — ném `{ error: 'discount_cap' }`
- Produces: `computeNetFromCsBh(listVnd, discountPct): number` — `Math.round(list * (1 - pct/100))`
- Produces: `assertOnePrice(onePrice, listVnd, discountPct, netVnd): void` — khi `onePrice` và `net !== compute` → ném `{ error: 'one_price' }`
- Produces: `netAfterVat(grossVnd, vatMode): number` — `excluded` → `Math.round(gross / 1.1)`; `included` → gross
- Produces: `maintenanceFeeTotal(feeVnd, feeUnit, areaM2): number` — `per_unit` → fee; `per_m2` → `Math.round(fee * areaM2)`

- [ ] **Step 1: Flags + util spec (RED)**

```ts
// bds.flags.spec.ts — restore PTT_BDS_POLICY in afterEach (delete if prev undefined)
it('defaults POLICY off when unset', () => {
  delete process.env.PTT_BDS_POLICY;
  expect(isBdsPolicyEnabled()).toBe(false);
});

it('POLICY on for 1', () => {
  process.env.PTT_BDS_POLICY = '1';
  expect(isBdsPolicyEnabled()).toBe(true);
});
```

```ts
// bds-policy.guard.spec.ts
it('404 when PACK off', () => {
  process.env.PTT_BDS_PACK = '0';
  process.env.PTT_BDS_POLICY = '1';
  expect(() => new BdsPolicyGuard().canActivate()).toThrow(NotFoundException);
});

it('404 when POLICY off', () => {
  process.env.PTT_BDS_PACK = '1';
  process.env.PTT_BDS_POLICY = '0';
  expect(() => new BdsPolicyGuard().canActivate()).toThrow(NotFoundException);
});

it('allows when PACK and POLICY on', () => {
  process.env.PTT_BDS_PACK = '1';
  process.env.PTT_BDS_POLICY = '1';
  expect(new BdsPolicyGuard().canActivate()).toBe(true);
});
```

```ts
// bds-policy.util.spec.ts
it('only cdt_sales_dir can activate', () => {
  expect(canActivatePolicy('cdt_sales_dir')).toBe(true);
  expect(canActivatePolicy('cv_gia')).toBe(false);
  expect(canActivatePolicy('')).toBe(false);
  expect(canActivatePolicy('  gdkd  ')).toBe(false);
});

it('BDS-12 discount over cap without approval throws discount_cap', () => {
  expect(() => assertDiscountAllowed(5, 8, false)).toThrow(
    expect.objectContaining({ error: 'discount_cap' }),
  );
});

it('discount at or under cap ok', () => {
  expect(() => assertDiscountAllowed(5, 5, false)).not.toThrow();
  expect(() => assertDiscountAllowed(5, 3, false)).not.toThrow();
});

it('discount over cap with approval ok', () => {
  expect(() => assertDiscountAllowed(5, 8, true)).not.toThrow();
});

it('computeNetFromCsBh rounds', () => {
  expect(computeNetFromCsBh(1_000_000_000, 5)).toBe(950_000_000);
});

it('assertOnePrice throws when project one_price and net mismatches', () => {
  expect(() => assertOnePrice(true, 100, 10, 95)).toThrow(
    expect.objectContaining({ error: 'one_price' }),
  );
  expect(() => assertOnePrice(true, 100, 10, 90)).not.toThrow();
  expect(() => assertOnePrice(false, 100, 10, 99)).not.toThrow();
});

it('vat excluded strips 10%', () => {
  expect(netAfterVat(110, 'excluded')).toBe(100);
  expect(netAfterVat(110, 'included')).toBe(110);
});

it('maintenance per_m2', () => {
  expect(maintenanceFeeTotal(100_000, 'per_m2', 50)).toBe(5_000_000);
  expect(maintenanceFeeTotal(2_000_000, 'per_unit', 50)).toBe(2_000_000);
});
```

- [ ] **Step 2: RED rồi implement**

```ts
export function isBdsPolicyEnabled(): boolean {
  return envFlagOn(process.env.PTT_BDS_POLICY);
}
```

Guard copy `BdsProjectOsGuard`, đổi `isBdsPolicyEnabled`.

```ts
// bds-policy.types.ts
export type PolicyStatus = 'draft' | 'active' | 'archived';
export type PolicyAudience = 'direct' | 'broker' | 'all';
export type VatMode = 'included' | 'excluded';
export type FeeUnit = 'per_unit' | 'per_m2';
```

```ts
// bds-policy.util.ts
export function canActivatePolicy(actorRole: string): boolean {
  return String(actorRole ?? '').trim() === 'cdt_sales_dir';
}

export function assertDiscountAllowed(
  capPct: number,
  requestedPct: number,
  approved: boolean,
): void {
  if (requestedPct > capPct && approved !== true) {
    throw { error: 'discount_cap' };
  }
}

export function computeNetFromCsBh(listVnd: number, discountPct: number): number {
  return Math.round(listVnd * (1 - discountPct / 100));
}

export function assertOnePrice(
  onePrice: boolean,
  listVnd: number,
  discountPct: number,
  netVnd: number,
): void {
  if (!onePrice) return;
  if (netVnd !== computeNetFromCsBh(listVnd, discountPct)) {
    throw { error: 'one_price' };
  }
}

export function netAfterVat(grossVnd: number, vatMode: VatMode): number {
  if (vatMode === 'excluded') return Math.round(grossVnd / 1.1);
  return grossVnd;
}

export function maintenanceFeeTotal(
  feeVnd: number,
  feeUnit: FeeUnit,
  areaM2: number,
): number {
  return feeUnit === 'per_m2' ? Math.round(feeVnd * areaM2) : feeVnd;
}
```

`AppConfigService`: `bdsPolicyEnabled` cùng kiểu `bdsHoldTtlEnabled` (`PTT_BDS_POLICY ?? '0'`).

Jest:

```bash
cd services/ptt-crm-api
./node_modules/.bin/jest src/bds/bds.flags.spec.ts src/bds/guards/bds-policy.guard.spec.ts src/bds/policies/bds-policy.util.spec.ts --runInBand
```

Expected: PASS.

- [ ] **Step 3: Commit** — chỉ khi user yêu cầu: `feat(bds): P3 POLICY flag and CSBH utils`

---

### Task 2: DDL CSBH + bảng giá PG

**Files:**
- Create: `docs/specs/postgresql-ddl-bds-p3.sql`
- Create: `scripts/apply_pg_ddl_bds_p3.sh` (copy `scripts/apply_pg_ddl_bds_p1b.sh`, đổi file + echo `OK  bds P3 DDL`)

Không sửa DDL P0/P1/P1b/P2.

- [ ] **Step 1: Write DDL**

```sql
-- Pack BĐS P3 — Apply: scripts/apply_pg_ddl_bds_p3.sh
BEGIN;

ALTER TABLE bds_launch_phases ADD COLUMN IF NOT EXISTS policy_id UUID;
ALTER TABLE bds_launch_phases ADD COLUMN IF NOT EXISTS price_list_id INTEGER;

CREATE TABLE IF NOT EXISTS bds_sales_policies (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES bds_tenants (id),
  project_id INTEGER NOT NULL REFERENCES crm_re_projects (id) ON DELETE CASCADE,
  code TEXT NOT NULL,
  name TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL DEFAULT 'draft'
    CHECK (status IN ('draft', 'active', 'archived')),
  audience TEXT NOT NULL DEFAULT 'all'
    CHECK (audience IN ('direct', 'broker', 'all')),
  effective_from TIMESTAMPTZ,
  effective_to TIMESTAMPTZ,
  discount_cap_pct NUMERIC NOT NULL DEFAULT 0,
  hold_ttl_minutes INTEGER,
  deposit_min_vnd BIGINT NOT NULL DEFAULT 0,
  vbtt_min_paid_pct NUMERIC NOT NULL DEFAULT 0,
  hdmb_min_paid_pct NUMERIC NOT NULL DEFAULT 30,
  payment_template_json JSONB NOT NULL DEFAULT '[]'::jsonb,
  vat_mode TEXT NOT NULL DEFAULT 'included'
    CHECK (vat_mode IN ('included', 'excluded')),
  maintenance_fee_vnd BIGINT NOT NULL DEFAULT 0,
  fee_unit TEXT NOT NULL DEFAULT 'per_unit'
    CHECK (fee_unit IN ('per_unit', 'per_m2')),
  rules_json JSONB NOT NULL DEFAULT '{}'::jsonb,
  activated_by TEXT NOT NULL DEFAULT '',
  activated_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE UNIQUE INDEX IF NOT EXISTS uq_bds_policies_project_code
  ON bds_sales_policies (project_id, lower(trim(code)));

CREATE UNIQUE INDEX IF NOT EXISTS uq_bds_policies_one_active_audience
  ON bds_sales_policies (project_id, audience)
  WHERE status = 'active';

CREATE TABLE IF NOT EXISTS crm_re_price_lists (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  project_id INTEGER NOT NULL REFERENCES crm_re_projects (id) ON DELETE CASCADE,
  tenant_id UUID REFERENCES bds_tenants (id),
  policy_id UUID REFERENCES bds_sales_policies (id),
  version_code TEXT NOT NULL DEFAULT '',
  name TEXT NOT NULL DEFAULT '',
  effective_date TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL DEFAULT 'draft'
    CHECK (status IN ('draft', 'active', 'archived')),
  notes TEXT NOT NULL DEFAULT '',
  applied_at TEXT NOT NULL DEFAULT '',
  applied_by TEXT NOT NULL DEFAULT '',
  created_by TEXT NOT NULL DEFAULT '',
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_crm_re_price_lists_version
  ON crm_re_price_lists (project_id, lower(trim(version_code)))
  WHERE trim(version_code) <> '';

CREATE TABLE IF NOT EXISTS crm_re_price_list_items (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  price_list_id INTEGER NOT NULL REFERENCES crm_re_price_lists (id) ON DELETE CASCADE,
  unit_code TEXT NOT NULL DEFAULT '',
  zone TEXT NOT NULL DEFAULT '',
  list_price_vnd BIGINT NOT NULL DEFAULT 0,
  net_price_vnd BIGINT NOT NULL DEFAULT 0,
  notes TEXT NOT NULL DEFAULT '',
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE UNIQUE INDEX IF NOT EXISTS idx_crm_re_price_list_items_unit
  ON crm_re_price_list_items (price_list_id, lower(trim(unit_code)))
  WHERE trim(unit_code) <> '';

COMMIT;
```

Nếu bảng `crm_re_price_lists` **đã tồn tại** trên PG (ít khả năng): `CREATE TABLE IF NOT EXISTS` no-op — thêm cột bằng:

```sql
ALTER TABLE crm_re_price_lists ADD COLUMN IF NOT EXISTS tenant_id UUID REFERENCES bds_tenants (id);
ALTER TABLE crm_re_price_lists ADD COLUMN IF NOT EXISTS policy_id UUID;
```

Gắn **sau** `CREATE TABLE` (idempotent trên DB trống lẫn DB đã có bảng).

Không ADD FK `bds_launch_phases.policy_id` → `bds_sales_policies` (tránh vòng khi archive). Service giữ đồng bộ.

- [ ] **Step 2: Apply local (idempotent ×2)**

```bash
export DATABASE_URL="${DATABASE_URL:-postgresql://ptt:ptt_dev@127.0.0.1:5433/rnosaidb}"
bash scripts/apply_pg_ddl_bds_p3.sh
bash scripts/apply_pg_ddl_bds_p3.sh
psql "$DATABASE_URL" -c '\d bds_sales_policies'
```

Expected: bảng + `uq_bds_policies_one_active_audience`; lần 2 NOTICE / OK. Nếu Docker postgres down: start `rnosai-postgres`.

- [ ] **Step 3: Commit** — chỉ khi user yêu cầu

---

### Task 3: Repo policy + price list + snapshot đợt

**Files:**
- Create: `services/ptt-crm-api/src/bds/policies/bds-policy.repository.ts`

**Interfaces:**
- Consumes: bảng Task 2
- Produces:
  - `insertPolicy(row): PolicyRow`
  - `updateDraft(id, patch): PolicyRow | null` — `WHERE id AND status='draft'`
  - `getPolicy(id): PolicyRow | null`
  - `listByProject(projectId): PolicyRow[]`
  - `setPolicyStatusIf(id, status, extra, expected): PolicyRow | null`
  - `archiveActiveAudience(projectId, audience, exceptId): number`
  - `insertPriceList(row): PriceListRow`
  - `getPriceList(id): PriceListRow | null`
  - `listPriceLists(projectId): PriceListRow[]`
  - `upsertPriceListItem(priceListId, item): PriceListItemRow`
  - `listPriceListItems(priceListId): PriceListItemRow[]`
  - `setPriceListPolicy(priceListId, policyId): void`
  - `getPhase(phaseId): { id, project_id } | null`
  - `setPhaseSnapshot(phaseId, policyId, priceListId): void`
  - `resolveProjectTenantId(projectId): string | null`
  - `getProjectOnePrice(projectId): boolean | null` — `null` = project không tồn tại

Pool pattern copy `BdsHoldRepository`:

```ts
constructor(private readonly config: AppConfigService) {}
private get db(): Pool { ... this.config.databaseUrl ... }
```

`PolicyRow` fields khớp cột Task 2 (`discount_cap_pct` đọc `Number(...)`).

`insertPolicy` / unique code: bắt `23505` → ném `{ code: '23505' }` (giống hold).

```ts
async setPolicyStatusIf(
  id: string,
  status: PolicyStatus,
  extra: { activated_by?: string },
  expected: PolicyStatus,
): Promise<PolicyRow | null> {
  const res = await this.db.query(
    `UPDATE bds_sales_policies
     SET status = $2,
         activated_by = COALESCE($3, activated_by),
         activated_at = CASE WHEN $2 = 'active' THEN NOW() ELSE activated_at END,
         updated_at = NOW()
     WHERE id = $1 AND status = $4
     RETURNING *`,
    [id, status, extra.activated_by ?? null, expected],
  );
  return res.rows[0] ? this.mapPolicy(res.rows[0]) : null;
}

async archiveActiveAudience(
  projectId: number,
  audience: PolicyAudience,
  exceptId: string,
): Promise<number> {
  const res = await this.db.query(
    `UPDATE bds_sales_policies
     SET status = 'archived', updated_at = NOW()
     WHERE project_id = $1 AND audience = $2 AND status = 'active' AND id <> $3`,
    [projectId, audience, exceptId],
  );
  return res.rowCount ?? 0;
}

async setPhaseSnapshot(phaseId: string, policyId: string, priceListId: number): Promise<void> {
  await this.db.query(
    `UPDATE bds_launch_phases
     SET policy_id = $2, price_list_id = $3
     WHERE id = $1`,
    [phaseId, policyId, priceListId],
  );
}

async getProjectOnePrice(projectId: number): Promise<boolean | null> {
  const res = await this.db.query(
    `SELECT one_price FROM crm_re_projects WHERE id = $1`,
    [projectId],
  );
  if (!res.rows[0]) return null;
  return Boolean(res.rows[0].one_price);
}
```

Không bắt buộc Jest repo (service mock). `tsc --noEmit` 0.

- [ ] **Step 1: Implement repo**
- [ ] **Step 2:** `./node_modules/.bin/tsc -p tsconfig.build.json --noEmit` exit 0

---

### Task 4: Service draft — CSBH + bảng giá

**Files:**
- Create: `services/ptt-crm-api/src/bds/policies/bds-policy.service.ts`
- Create: `services/ptt-crm-api/src/bds/policies/bds-policy.service.spec.ts`
- Modify: `bds.module.ts` — providers `BdsPolicyRepository`, `BdsPolicyService`; export service. **Không** controller ở task này.

**Interfaces:**
- Consumes: repo Task 3
- Produces:
  - `create(projectId, body, tenantId?): PolicyRow` — 201-shape, status luôn `draft`
  - `updateDraft(id, body, tenantId?): PolicyRow`
  - `get(id, tenantId?): PolicyRow`
  - `listByProject(projectId, tenantId?): PolicyRow[]`
  - `createPriceList(projectId, body, tenantId?): PriceListRow`
  - `addPriceListItem(priceListId, body, tenantId?): PriceListItemRow`
  - `listPriceLists(projectId, tenantId?): PriceListRow[]`

`CreatePolicyBody`:

```ts
{
  code: string;
  name?: string;
  audience?: PolicyAudience;          // default 'all'
  discount_cap_pct?: number;          // default 0
  hold_ttl_minutes?: number | null;
  deposit_min_vnd?: number;
  vbtt_min_paid_pct?: number;
  hdmb_min_paid_pct?: number;
  payment_template_json?: unknown;
  vat_mode?: VatMode;
  maintenance_fee_vnd?: number;
  fee_unit?: FeeUnit;
  rules_json?: unknown;
  effective_from?: string | null;
  effective_to?: string | null;
}
```

`code` trim rỗng → 400 `{ error: 'code' }`.  
`audience` không thuộc `direct|broker|all` → 400 `{ error: 'audience' }`.  
`discount_cap_pct` < 0 hoặc > 100 → 400 `{ error: 'discount_cap_pct' }`.

Tenant: `resolveProjectTenantId`; nếu `tenantId` header khác project → 404 (không PII). Project không tồn tại (`resolve` + `getProjectOnePrice` đều null) → 404.

`insertPolicy` 23505 → 409 `{ error: 'policy_code' }`.

`updateDraft` miss (không draft / không id) → 409 `{ error: 'policy_locked' }`. Không cho PATCH `status`.

`createPriceList`: `version_code` trim bắt buộc; 23505 → 409 `{ error: 'version_code' }`. Status luôn `draft`. `policy_id` null đến lúc activate.

`addPriceListItem`: list phải cùng project (qua `getPriceList`); list `active` → 409 `{ error: 'price_list_locked' }` (không sửa items sau snapshot). `unit_code` trim bắt buộc.

- [ ] **Step 1: Spec (RED)**

```ts
const repo = {
  insertPolicy: jest.fn(),
  updateDraft: jest.fn(),
  getPolicy: jest.fn(),
  listByProject: jest.fn(),
  resolveProjectTenantId: jest.fn().mockResolvedValue('t1'),
  getProjectOnePrice: jest.fn().mockResolvedValue(true),
  insertPriceList: jest.fn(),
  getPriceList: jest.fn(),
  upsertPriceListItem: jest.fn(),
  listPriceLists: jest.fn(),
  // stubs Task 5
  setPolicyStatusIf: jest.fn(),
  archiveActiveAudience: jest.fn(),
  getPhase: jest.fn(),
  setPhaseSnapshot: jest.fn(),
  setPriceListPolicy: jest.fn(),
};

it('create stamps tenant from project not body', async () => {
  repo.insertPolicy.mockImplementation(async (row) => ({ id: 'p1', ...row, status: 'draft' }));
  const svc = new BdsPolicyService(repo as never);
  await svc.create(9, { code: 'CSBH-1', tenant_id: 'evil' } as never, 't1');
  expect(repo.insertPolicy).toHaveBeenCalledWith(
    expect.objectContaining({ tenant_id: 't1', project_id: 9, status: 'draft', code: 'CSBH-1' }),
  );
});

it('create empty code → 400', async () => {
  const svc = new BdsPolicyService(repo as never);
  await expect(svc.create(9, { code: '  ' }, 't1')).rejects.toMatchObject({
    response: { error: 'code' },
  });
});

it('wrong tenant header → 404', async () => {
  const svc = new BdsPolicyService(repo as never);
  await expect(svc.create(9, { code: 'A' }, 'other')).rejects.toBeInstanceOf(NotFoundException);
});

it('update non-draft → 409 policy_locked', async () => {
  repo.updateDraft.mockResolvedValue(null);
  repo.getPolicy.mockResolvedValue({ id: 'p1', project_id: 9, tenant_id: 't1', status: 'active' });
  const svc = new BdsPolicyService(repo as never);
  await expect(svc.updateDraft('p1', { name: 'x' }, 't1')).rejects.toMatchObject({
    response: { error: 'policy_locked' },
  });
});
```

Map Nest: `BadRequestException({ error })` / `ConflictException({ error })` / `NotFoundException()`. Jest `toMatchObject({ response: { error } })` hoặc `getResponse()`.

- [ ] **Step 2: Implement + Jest `src/bds/policies/bds-policy.service.spec.ts --runInBand` xanh**
- [ ] **Step 3: Commit** — chỉ khi user yêu cầu

---

### Task 5: Activate + quote — UC-009 / BDS-12

**Files:**
- Modify: `bds-policy.service.ts` + spec

**Interfaces:**
- Produces:
  - `activate(id, body, tenantId?): PolicyRow`
  - `archive(id, body, tenantId?): PolicyRow`
  - `quote(id, body, tenantId?): { list_price_vnd, discount_pct, net_price_vnd, maintenance_fee_vnd }`

`ActivateBody`:

```ts
{
  phase_id: string;
  price_list_id: number;
  actor_role: string;       // bắt buộc
  activated_by?: string;
}
```

Thứ tự `activate`:

1. `get` policy (404 tenant).
2. `canActivatePolicy(actor_role)` false → 403 `{ error: 'activate_forbidden' }` (UC-009 E1). **Không** default-allow khi thiếu role.
3. `status === 'archived'` → 409 `{ error: 'policy_closed' }`. `status === 'active'` → 409 `{ error: 'already_active' }`.
4. `phase_id` trim rỗng → 400 `{ error: 'phase_id' }`. `getPhase` null hoặc `phase.project_id !== policy.project_id` → 404.
5. `price_list_id` không hữu hạn → 400 `{ error: 'price_list_id' }`. List null / khác project → 404.
6. `archiveActiveAudience(project_id, audience, id)`.
7. `setPolicyStatusIf(id, 'active', { activated_by }, 'draft')`. Miss → 409 `{ error: 'policy_locked' }`.
8. `setPhaseSnapshot(phase_id, id, price_list_id)`.
9. `setPriceListPolicy(price_list_id, id)` (cột `policy_id`; **không** bắt buộc đổi status list — snapshot là phase).
10. Return row active.

`archive`: cùng `actor_role` rule. `setPolicyStatusIf(..., 'archived', {}, expected)` với `expected` = status hiện tại nếu `draft|active`. Miss → 409 `{ error: 'policy_closed' }`. Không xóa `phase.policy_id` (lịch sử đợt). Unique index nhả chỗ cho CSBH mới.

`QuoteBody`:

```ts
{
  list_price_vnd: number;
  discount_pct: number;
  discount_approved?: boolean;
  net_price_vnd?: number;     // optional — nếu có và project.one_price → assertOnePrice
  area_m2?: number;
}
```

`quote`:

1. Load policy (404).
2. `list` / `discount_pct` không finite → 400 `{ error: 'quote' }`.
3. `assertDiscountAllowed(policy.discount_cap_pct, discount_pct, !!discount_approved)` — bắt object `{ error: 'discount_cap' }` → `BadRequestException`.
4. `net = computeNetFromCsBh(...)`.
5. Nếu `net_price_vnd` là số: `assertOnePrice(getProjectOnePrice ?? true, ...)`.
6. `maintenance = maintenanceFeeTotal(policy.maintenance_fee_vnd, policy.fee_unit, area_m2 ?? 0)`.
7. Return `{ list_price_vnd, discount_pct, net_price_vnd: net, maintenance_fee_vnd }`.

- [ ] **Step 1: Spec (RED)**

```ts
it('UC-009 E1 cv_gia cannot activate', async () => {
  repo.getPolicy.mockResolvedValue({
    id: 'p1', project_id: 9, tenant_id: 't1', status: 'draft', audience: 'all',
  });
  const svc = new BdsPolicyService(repo as never);
  await expect(
    svc.activate('p1', { phase_id: 'ph1', price_list_id: 1, actor_role: 'cv_gia' }, 't1'),
  ).rejects.toMatchObject({ response: { error: 'activate_forbidden' } });
});

it('activate snapshots phase + archives sibling', async () => {
  repo.getPolicy.mockResolvedValue({
    id: 'p1', project_id: 9, tenant_id: 't1', status: 'draft', audience: 'all',
  });
  repo.getPhase.mockResolvedValue({ id: 'ph1', project_id: 9 });
  repo.getPriceList.mockResolvedValue({ id: 3, project_id: 9 });
  repo.setPolicyStatusIf.mockResolvedValue({ id: 'p1', status: 'active' });
  const svc = new BdsPolicyService(repo as never);
  await svc.activate(
    'p1',
    { phase_id: 'ph1', price_list_id: 3, actor_role: 'cdt_sales_dir', activated_by: 'gdkd' },
    't1',
  );
  expect(repo.archiveActiveAudience).toHaveBeenCalledWith(9, 'all', 'p1');
  expect(repo.setPhaseSnapshot).toHaveBeenCalledWith('ph1', 'p1', 3);
  expect(repo.setPriceListPolicy).toHaveBeenCalledWith(3, 'p1');
});

it('BDS-12 quote over cap → 400 discount_cap', async () => {
  repo.getPolicy.mockResolvedValue({
    id: 'p1', project_id: 9, tenant_id: 't1', status: 'active',
    discount_cap_pct: 5, maintenance_fee_vnd: 0, fee_unit: 'per_unit',
  });
  const svc = new BdsPolicyService(repo as never);
  await expect(
    svc.quote('p1', { list_price_vnd: 100, discount_pct: 8 }, 't1'),
  ).rejects.toMatchObject({ response: { error: 'discount_cap' } });
});

it('quote under cap returns net', async () => {
  repo.getPolicy.mockResolvedValue({
    id: 'p1', project_id: 9, tenant_id: 't1', status: 'draft',
    discount_cap_pct: 5, maintenance_fee_vnd: 0, fee_unit: 'per_unit',
  });
  const svc = new BdsPolicyService(repo as never);
  await expect(
    svc.quote('p1', { list_price_vnd: 100, discount_pct: 5 }, 't1'),
  ).resolves.toMatchObject({ net_price_vnd: 95 });
});

it('quote net mismatch when one_price → 400 one_price', async () => {
  repo.getPolicy.mockResolvedValue({
    id: 'p1', project_id: 9, tenant_id: 't1', status: 'active',
    discount_cap_pct: 10, maintenance_fee_vnd: 0, fee_unit: 'per_unit',
  });
  repo.getProjectOnePrice.mockResolvedValue(true);
  const svc = new BdsPolicyService(repo as never);
  await expect(
    svc.quote('p1', { list_price_vnd: 100, discount_pct: 10, net_price_vnd: 99 }, 't1'),
  ).rejects.toMatchObject({ response: { error: 'one_price' } });
});
```

403: `ForbiddenException({ error: 'activate_forbidden' })`.

- [ ] **Step 2: Implement + Jest `src/bds/policies --runInBand` xanh**

---

### Task 6: HTTP + module

**Files:**
- Create: `services/ptt-crm-api/src/bds/policies/bds-policy.controller.ts`
- Create: `services/ptt-crm-api/src/bds/policies/bds-policy.controller.spec.ts` (thin: instantiate + `create` / `activate` delegate)
- Modify: `bds.module.ts` — `BdsPolicyController` + `BdsPolicyGuard` provider

**Guards:** `@UseGuards(StaffOrInternalKeyGuard, BdsPackGuard, BdsPolicyGuard)`.

| Method | Path | HttpCode |
|--------|------|----------|
| GET | `/api/v1/bds/projects/:id/policies` | 200 |
| POST | `/api/v1/bds/projects/:id/policies` | **201** |
| GET | `/api/v1/bds/policies/:id` | 200 |
| POST | `/api/v1/bds/policies/:id/update` | 200 |
| POST | `/api/v1/bds/policies/:id/activate` | 200 |
| POST | `/api/v1/bds/policies/:id/archive` | 200 |
| POST | `/api/v1/bds/policies/:id/quote` | 200 |
| GET | `/api/v1/bds/projects/:id/price-lists` | 200 |
| POST | `/api/v1/bds/projects/:id/price-lists` | **201** |
| POST | `/api/v1/bds/price-lists/:id/items` | **201** |

`:id` project / price-list = `ParseIntPipe` (price-list items: `ParseIntPipe`). Policy id = UUID string.

Dùng `POST .../update` thay PATCH — đồng bộ hold approve (tránh Nest verb lạ trên nginx).

```ts
@Controller('api/v1/bds')
@UseGuards(StaffOrInternalKeyGuard, BdsPackGuard, BdsPolicyGuard)
export class BdsPolicyController {
  constructor(private readonly policies: BdsPolicyService) {}

  @Post('projects/:id/policies')
  create(
    @Param('id', ParseIntPipe) id: number,
    @Body() body: CreatePolicyBody,
    @Headers('x-bds-tenant') tenantId?: string,
  ) {
    return this.policies.create(id, body, tenantId);
  }

  @Post('policies/:id/activate')
  @HttpCode(HttpStatus.OK)
  activate(
    @Param('id') id: string,
    @Body() body: { phase_id?: string; price_list_id?: number; actor_role?: string; activated_by?: string },
    @Headers('x-bds-tenant') tenantId?: string,
  ) {
    return this.policies.activate(id, {
      phase_id: String(body.phase_id ?? ''),
      price_list_id: Number(body.price_list_id),
      actor_role: String(body.actor_role ?? ''),
      activated_by: body.activated_by,
    }, tenantId);
  }
}
```

Header `x-bds-tenant` chỉ auth/404 — **không** persist.

- [ ] **Step 1: Register; `tsc --noEmit` 0; Jest `src/bds --runInBand` xanh**
- [ ] **Step 2: PACK=0 hoặc POLICY=0 → HTTP policy 404 (sau auth). Guard spec đủ; controller thin**

---

### Task 7: Roadmap + DoD

**Files:**
- Modify: `docs/superpowers/plans/2026-08-22-bds-coding-roadmap.md`

- [ ] **Step 1: Hàng P3**

| Cột | Giá trị |
|-----|---------|
| Plan file | `[bds-p3-csbh.md](./2026-08-22-bds-p3-csbh.md)` |
| Thắng | `BDS-12` (activate `cdt_sales_dir`; one_price util) |

Mục «### P3 — CSBH» giữ: `bds_sales_policies`, activate chỉ `cdt_sales_dir`, snapshot giá đợt. Test BDS-12. Thêm một dòng: BDS-33 HTTP = P4/P5.

Flag table §4: thêm hàng `PTT_BDS_POLICY` sau HOLD_TTL — mặc định 0; staging bật khi PACK=1 + P1b.

- [ ] **Step 2: Tự soi spec** — BDS-12 có test; UC-009 E1 có test; không convert-deposit; không UI.

---

## 4. Definition of Done P3

- [ ] Jest flags + policy util + policy service + guard + controller xanh
- [ ] `tsc` build api 0
- [ ] DDL P3 apply idempotent
- [ ] BDS-12: quote `discount_pct > cap` không `discount_approved` → 400 `{ error: 'discount_cap' }`
- [ ] UC-009: `actor_role=cdt_sales_dir` + phase + price list → policy `active`, phase snapshot
- [ ] UC-009 E1: `cv_gia` activate → 403 `{ error: 'activate_forbidden' }`
- [ ] Một audience một `active` (archive sibling + unique index)
- [ ] `one_price` util: net lệch → `{ error: 'one_price' }` (quote khi gửi `net_price_vnd`)
- [ ] PACK=0 hoặc POLICY=0 → HTTP CSBH 404
- [ ] Prod không bật PACK / POLICY
- [ ] Không TX / không agency / không UI `/crm/bds`

---

## 5. Rollback

`PTT_BDS_PACK=0` và/hoặc `PTT_BDS_POLICY=0`. Không DROP `bds_sales_policies` trên prod.

---

## 6. Sau P3 xanh

P4 TX + BDS-11 convert + snapshot `policy_id` lúc cọc. P4b mới chặn `%` HĐMB. P5 mới BDS-33 HTTP đại lý. P10 khóa giá launch. P8 UI tab Giá/CSBH + cap JWT.

---

*P3 không phải Transaction OS. Thắng: soạn draft; GĐKD bật CSBH gắn đợt; saler không tự vượt cap.*
