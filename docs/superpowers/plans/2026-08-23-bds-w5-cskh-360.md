# W5 — Board `re_buyer` + 360 ẩn Deal Room + after intake

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `cskh_lead` chạy SOP trên `/crm/cskh-board?flow=re_buyer` (SLA 15p, cột căn/hold/TX); mở 360 `/crm/leads/:id` skin `re_buyer` không Deal Room; `truong_after` thấy hàng after ngay khi TX `contracted`.

**Architecture:** Reuse-first. Một board (`CskhBoardContent` + `GET /api/crm/cskh-board`) — thêm `flow=re_buyer` vào filter SQL **đã có**. Một 360 (`leads/[id]`) — hide Deal Room bằng helper **đã có**; payload join qua `GET /api/v1/bds/spine/buyer/:leadId` (W4 cấm, W5 được). After = TX `contracted` đã nằm trong `listBoard`; W5 chỉ hook idempotent seed checklist. Không board component mới, không `Bds2Module`, không Kafka.

**Tech Stack:** NestJS + Jest (`ptt-crm-api`); Next.js + Vitest (`ops-web`).

**Spec:** [2026-08-23-bds-role-feature-execution.md](../specs/2026-08-23-bds-role-feature-execution.md) §3.10 CS-01–CS-06, §3.16 AF-01, §7 W5  
**Unification:** [2026-08-23-bds-crm-os-unification-design.md](../specs/2026-08-23-bds-crm-os-unification-design.md) Q33, Q46, §5–6, U-02, U-03  
**UX:** [2026-08-23-bds-ux-ui-complete.md](../specs/2026-08-23-bds-ux-ui-complete.md) SCR-BDS-041 / 042  
**OS plan:** [2026-08-23-bds-os-coding.md](./2026-08-23-bds-os-coding.md) Task 14–15  
**W4 (xong):** [2026-08-23-bds-w4-spine.md](./2026-08-23-bds-w4-spine.md)

## Global Constraints

- Không thay Q1–Q48. Không rewrite `CskhBoardContent` thành kanban mới. Không page `/crm/bds-v2`.
- Không `Bds2Module`, không ẩn B2B nav (W6), không war-room +2 widget (W6), không finance/CAPI (W7), không offboard (W8).
- **Cấm** Kafka, **cấm** `bds_spine_events`, **cấm** `PTT_BDS_OS` / `PTT_BDS_CSKH_BOARD` flag mới. Gate bằng `PTT_BDS_PACK` + `PTT_BDS_BUYER` đã có.
- **Cấm** helper hide Deal Room thứ hai — chỉ `shouldHideDealRoom`.
- Default board **không** `flow` = giữ `spa_meta_only` (Meta/Facebook / `agency_client_id`). `flow=re_buyer` mới tắt spa filter + `buildReBuyerListFilter`.
- `cskh_lead` **không** có `crm_leads.view` / `crm_board.view` — chỉ `bds_buyers.*`. W5 phải mở **đúng** board + 360, không mở list/export SPA.
- Home-summary block `re_buyer` (U-11) = **W6**, không W5.
- Hẹn BG 15N (AF-02) = UI after đã có; W5 **không** auto set `handover_appointment_at`.
- Flag staging: không tắt `PTT_BDS_PACK` / `PTT_BDS_BUYER` / `PTT_BDS_UI` / `PTT_STAFF_TICKETS`.
- Test Nest: `cd services/ptt-crm-api && ./node_modules/.bin/jest <file> --runInBand`
- Test FE: `cd services/ops-web && ./node_modules/.bin/vitest run <file>`
- Không commit trừ khi user yêu cầu.

### Gap hôm nay (khóa — đúng code)

| Chỗ | Thực tế |
|-----|---------|
| `GET /api/crm/cskh-board` | Không đọc `flow`. `spa_meta_only` mặc định **true** → loại lead BĐS (trừ ads Meta). |
| Guard board | `StaffLeadsViewGuard` = `crm_leads.view` → `cskh_lead` **403**. |
| FE `/crm/cskh-board` | `rbac-routes` cần `crm_board.*` → `cskh_lead` **không vào page**. |
| `fetchCskhBoard` | Không gửi `flow`. |
| Nav «Lead khách mua» | `/crm/bds/leads` (list W1) — chưa phải nhà board. |
| FE `lib/crm/lead-flow-kind.ts` | **Không có** `re_buyer` → lead BĐS resolve thành `b2b_prospect` → banner Deal Room **hiện**. |
| `leads/[id]/page.tsx` | Banner Deal Room theo `showB2bFlow`; **chưa** gọi `shouldHideDealRoom`. |
| Deal Room API / page | Đã 404 `re_buyer` (P6/P8). Giữ. |
| `GET /api/v1/bds/spine/buyer/:leadId` | Chưa có (W4 cấm). |
| After `listBoard` | SQL `stage IN (contracted, handed_over, title_issued)` — row **tự hiện** sau `contract()`. Chưa seed 4 checklist. |
| `contract()` | Ticket HH + CAPI; **không** gọi after. |

### Cap W5

| Bề mặt | Cap |
|--------|-----|
| Board `flow=re_buyer` | `bds_buyers.view` **hoặc** `crm_leads.view` |
| User chỉ `bds_buyers.view` | Force `flow=re_buyer` (không xem SPA) |
| User chỉ `crm_leads.view` + không `flow` | Board SPA như cũ |
| 360 `/crm/leads/:id` (Next rbac) | `crm_leads.view` **hoặc** `bds_buyers.view` |
| `GET /api/v1/bds/spine/buyer/:leadId` | `BdsBuyerGuard` (pack + `bds_buyers.view`) |
| Phone đầy đủ trên spine | `bds_buyers.view_pii`; thiếu → mask |
| After board | `bds_aftersales.view` (đã có) |

### Ngoài W5 (cấm trong PR này)

- Ẩn `/crm/sales`, `PTT_BDS_NAV_HIDE_B2B`, hub widget CSKH breach / thu hôm nay (W6).
- Finance hub, CAPI Purchase value (W7). Offboard (W8).
- Kanban 7 cột mới (UX 042 mô tả cột; W5 **thêm cột** trên bảng hiện tại — không rewrite card layout).
- Tab Chat/Việc mới trên 360 — deep-link `/crm/work` nếu tickets ON.
- Đổi contract hold/TX HTTP (W1). Đổi commissions (W3). Đổi spine key format (W4).

---

## File map

```
services/ops-web/src/lib/crm/lead-flow-kind.ts                    NÂNG — thêm re_buyer
services/ops-web/src/lib/crm/lead-flow-kind.spec.ts               NÂNG hoặc CREATE
services/ops-web/src/app/crm/leads/[id]/page.tsx                  NÂNG — shouldHideDealRoom + panel
services/ops-web/src/lib/bds/BdsBuyer360Panel.tsx                 CREATE
services/ops-web/src/lib/bds/api.ts                               NÂNG — fetchBdsSpineBuyer
services/ops-web/src/lib/bds/api.spec.ts                          NÂNG
services/ops-web/src/lib/bds/types.ts                             NÂNG — BdsSpineBuyer
services/ops-web/src/lib/bds/nav.ts                               NÂNG — href board
services/ops-web/src/lib/bds/nav.spec.ts                          NÂNG
services/ops-web/src/lib/rbac-routes.ts                           NÂNG — board + /crm/leads
services/ops-web/src/lib/auth.spec.ts                             NÂNG
services/ops-web/src/lib/api.ts                                   NÂNG — fetchCskhBoard flow
services/ops-web/src/app/crm/cskh-board/CskhBoardContent.tsx      NÂNG — ?flow= + cột BĐS
services/ops-web/src/app/crm/bds/leads/page.tsx                   NÂNG — link board + ?lead= → 360

services/ptt-crm-api/src/cskh-board/cskh-board-flow.util.ts       CREATE — resolve flow + cap
services/ptt-crm-api/src/cskh-board/cskh-board-flow.util.spec.ts  CREATE
services/ptt-crm-api/src/cskh-board/cskh-board.types.ts           NÂNG — flow + cột optional
services/ptt-crm-api/src/cskh-board/cskh-board.controller.ts      NÂNG — query flow + cap OR
services/ptt-crm-api/src/cskh-board/cskh-board.service.ts         NÂNG — filter + enrich
services/ptt-crm-api/src/cskh-board/cskh-board.repository.ts      NÂNG — WHERE re_buyer
services/ptt-crm-api/src/cskh-board/cskh-board.module.ts          NÂNG — import BdsModule
services/ptt-crm-api/src/bds/spine/bds-buyer-query.ts            CREATE — join hold/tx/unit
services/ptt-crm-api/src/bds/spine/bds-buyer-query.spec.ts       CREATE
services/ptt-crm-api/src/bds/spine/bds-spine-buyer.controller.ts CREATE — GET buyer/:leadId
services/ptt-crm-api/src/bds/hold/bds-hold.repository.ts         NÂNG — latestByLeadIds
services/ptt-crm-api/src/bds/transactions/bds-tx.repository.ts    NÂNG — latestByLeadIds
services/ptt-crm-api/src/bds/bds.module.ts                       NÂNG — controller + export query
services/ptt-crm-api/src/bds/aftersales/bds-aftersales.service.ts NÂNG — ensureIntake
services/ptt-crm-api/src/bds/aftersales/bds-aftersales.service.spec.ts NÂNG
services/ptt-crm-api/src/bds/transactions/bds-tx.service.ts       NÂNG — gọi ensureIntake
services/ptt-crm-api/src/bds/transactions/bds-tx.service.spec.ts  NÂNG
```

API **GIỮ** (không thêm route trừ 1 GET spine):

| Việc | Đã có |
|------|--------|
| Filter SQL `re_buyer` | `buildReBuyerListFilter` / `buildLeadFlowKindListFilter` |
| SLA 15p `re_buyer` | `LeadSlaCareService` |
| Hide Deal Room FE | `shouldHideDealRoom` + `deal-room/page.tsx` |
| Deal Room API 404 | `DealRoomService` |
| After board | `BdsAftersalesService.listBoard` |
| Ticket first-touch | W4 `replayHandoffTicket` `buyer.created` |
| List lead W1 | `/crm/bds/leads` + `GET /api/v1/bds/leads` |

API **THÊM** (đúng unification §12.2):

| Method | Path | Guard |
|--------|------|--------|
| GET | `/api/v1/bds/spine/buyer/:leadId` | Pack + `bds_buyers.view` |

`GET /api/crm/cskh-board?flow=re_buyer` = **cùng** route, thêm query.

---

### Task 1: Skin `re_buyer` + ẩn Deal Room trên 360

**Files:**
- Modify: `services/ops-web/src/lib/crm/lead-flow-kind.ts`
- Create: `services/ops-web/src/lib/crm/lead-flow-kind.spec.ts`
- Modify: `services/ops-web/src/app/crm/leads/[id]/page.tsx` (banner ~802–814)
- Test: `services/ops-web/src/lib/bds/deal-room-hide.spec.ts` (đã xanh — không đổi helper)

**Interfaces:**
- Consumes: `shouldHideDealRoom({ leadFlowKind, user })`
- Produces: `LeadFlowKind` gồm `'re_buyer'`; `showB2bSalesFlowBar('re_buyer') === false`

- [ ] **Step 1: Failing test — FE classify `re_buyer`**

Tạo spec Vitest:

```ts
import { describe, expect, it } from 'vitest';
import {
  resolveLeadFlowKind,
  showB2bSalesFlowBar,
  statusOptionsForFlowKind,
} from './lead-flow-kind';

describe('lead-flow-kind re_buyer', () => {
  it('classifies explicit re_buyer', () => {
    expect(resolveLeadFlowKind({ metaJson: { lead_flow_kind: 're_buyer' } })).toBe('re_buyer');
  });

  it('classifies re_project_id as re_buyer', () => {
    expect(resolveLeadFlowKind({ metaJson: { re_project_id: 12 } })).toBe('re_buyer');
  });

  it('hides B2B bar on re_buyer', () => {
    expect(showB2bSalesFlowBar('re_buyer')).toBe(false);
  });

  it('allows xem_nha / giu_cho', () => {
    expect(statusOptionsForFlowKind('re_buyer')).toContain('xem_nha');
    expect(statusOptionsForFlowKind('re_buyer')).toContain('giu_cho');
  });
});
```

- [ ] **Step 2: Run — expect FAIL**

```bash
cd services/ops-web && ./node_modules/.bin/vitest run src/lib/crm/lead-flow-kind.spec.ts
```

Expected: FAIL (`re_buyer` not in union / resolve returns `b2b_prospect`).

- [ ] **Step 3: Minimal implementation**

Trong `lead-flow-kind.ts`:

```ts
export type LeadFlowKind = 'spa_operational' | 'b2b_prospect' | 're_buyer';
```

Trong `resolveLeadFlowKind`, **trước** nhánh spa/b2b (khớp Nest `lead-flow-kind.util.ts`):

```ts
  const explicit = norm(String(meta.lead_flow_kind ?? meta.lead_flow ?? ''));
  if (explicit === 're_buyer' || explicit === 're-buyer' || explicit === 'bds') {
    return 're_buyer';
  }
  if (meta.re_project_id != null && String(meta.re_project_id).trim() !== '') {
    return 're_buyer';
  }
```

`leadFlowKindLabel('re_buyer')` → `'Khách mua BĐS'`.

```ts
const RE_BUYER_STATUSES = [
  'moi', 'da_lien_he', 'xem_nha', 'giu_cho', 'dat_coc', 'vbtt', 'hdmb', 'lost', 'pending_cleanup',
] as const;

export function statusOptionsForFlowKind(kind: LeadFlowKind): readonly string[] {
  if (kind === 're_buyer') return RE_BUYER_STATUSES;
  return kind === 'spa_operational' ? SPA_OPERATIONAL_STATUSES : B2B_PROSPECT_STATUSES;
}
```

`showPresalesForFlow` / `showContractForFlow` / `showB2bSalesFlowBar` giữ `kind === 'b2b_prospect'` (re_buyer = false).

`resolveLeadFlowKindFromLead`: nếu `funnel.lead_flow_kind === 're_buyer'` trả về `'re_buyer'`. Truyền `metaJson` từ lead nếu `LeadRow` có `meta_json` / `meta` — nếu không có field, explicit kind vẫn tới từ `statusOptionsApi.lead_flow_kind` trên page.

- [ ] **Step 4: Banner 360 — `shouldHideDealRoom`**

Trong `leads/[id]/page.tsx` chỗ banner Deal Room (~802), **không** xóa `showB2bFlow`. Bọc thêm:

```tsx
{accessToken &&
showB2bFlow &&
dealRoomEnabled() &&
funnelSnap?.presales &&
!shouldHideDealRoom({ leadFlowKind, user }) ? (
  <div className="deal-room-entry-banner">
    {/* existing copy */}
    <Link href={`/crm/leads/${leadId}/deal-room`} className="btn btn-sm btn-primary">
      Mở Deal Room →
    </Link>
  </div>
) : null}
```

Import `shouldHideDealRoom` from `@/lib/bds/deal-room-hide`. **Cấm** viết `if (leadFlowKind === 're_buyer')` song song — helper đã cover.

- [ ] **Step 5: Run tests**

```bash
cd services/ops-web && ./node_modules/.bin/vitest run src/lib/crm/lead-flow-kind.spec.ts src/lib/bds/deal-room-hide.spec.ts
```

Expected: PASS.

- [ ] **Step 6: Commit** (chỉ khi user yêu cầu)

```bash
git add services/ops-web/src/lib/crm/lead-flow-kind.ts \
  services/ops-web/src/lib/crm/lead-flow-kind.spec.ts \
  services/ops-web/src/app/crm/leads/[id]/page.tsx
git commit -m "$(cat <<'EOF'
fix(bds): classify re_buyer on lead 360 and hide Deal Room

EOF
)"
```

---

### Task 2: Board `flow=re_buyer` + cap OR

**Files:**
- Create: `services/ptt-crm-api/src/cskh-board/cskh-board-flow.util.ts`
- Create: `services/ptt-crm-api/src/cskh-board/cskh-board-flow.util.spec.ts`
- Modify: `services/ptt-crm-api/src/cskh-board/cskh-board.types.ts`
- Modify: `services/ptt-crm-api/src/cskh-board/cskh-board.controller.ts`
- Modify: `services/ptt-crm-api/src/cskh-board/cskh-board.repository.ts`
- Modify: `services/ptt-crm-api/src/cskh-board/cskh-board.service.ts` (`getBoard` query)
- Modify: `services/ops-web/src/lib/api.ts` (`fetchCskhBoard`)
- Modify: `services/ops-web/src/lib/rbac-routes.ts`
- Modify: `services/ops-web/src/lib/auth.spec.ts`
- Modify: `services/ops-web/src/lib/bds/nav.ts`
- Modify: `services/ops-web/src/lib/bds/nav.spec.ts`
- Modify: `services/ops-web/src/app/crm/cskh-board/CskhBoardContent.tsx` (đọc `?flow=`, gửi API — cột BĐS = Task 4)
- Modify: `services/ops-web/src/app/crm/bds/leads/page.tsx` (link «Mở board CSKH»)

**Interfaces:**
- Consumes: `buildLeadFlowKindListFilter('re_buyer', 'postgres', 'l')`
- Produces: `resolveCskhBoardFlow(...)` → `'re_buyer' | 'spa'`; controller 403 nếu `null`

- [ ] **Step 1: Failing test — resolve flow**

```ts
import { resolveCskhBoardFlow } from './cskh-board-flow.util';

describe('resolveCskhBoardFlow', () => {
  it('U-02 requested re_buyer with bds_buyers', () => {
    expect(
      resolveCskhBoardFlow({
        requested: 're_buyer',
        hasCrmLeadsView: false,
        hasBdsBuyersView: true,
      }),
    ).toBe('re_buyer');
  });

  it('forces re_buyer when only bds_buyers and no flow', () => {
    expect(
      resolveCskhBoardFlow({
        requested: undefined,
        hasCrmLeadsView: false,
        hasBdsBuyersView: true,
      }),
    ).toBe('re_buyer');
  });

  it('keeps spa default for crm_leads without flow', () => {
    expect(
      resolveCskhBoardFlow({
        requested: undefined,
        hasCrmLeadsView: true,
        hasBdsBuyersView: false,
      }),
    ).toBe('spa');
  });

  it('forbids board when neither cap', () => {
    expect(
      resolveCskhBoardFlow({
        requested: 're_buyer',
        hasCrmLeadsView: false,
        hasBdsBuyersView: false,
      }),
    ).toBeNull();
  });
});
```

- [ ] **Step 2: Run Nest spec — expect FAIL**

```bash
cd services/ptt-crm-api && ./node_modules/.bin/jest src/cskh-board/cskh-board-flow.util.spec.ts --runInBand
```

- [ ] **Step 3: Implement util + repository filter**

`cskh-board-flow.util.ts`:

```ts
export type CskhBoardFlow = 're_buyer' | 'spa';

export function resolveCskhBoardFlow(input: {
  requested?: string | null;
  hasCrmLeadsView: boolean;
  hasBdsBuyersView: boolean;
}): CskhBoardFlow | null {
  const req = String(input.requested ?? '').trim();
  if (req === 're_buyer') {
    return input.hasCrmLeadsView || input.hasBdsBuyersView ? 're_buyer' : null;
  }
  if (input.hasCrmLeadsView) return 'spa';
  if (input.hasBdsBuyersView) return 're_buyer';
  return null;
}
```

`CskhBoardQuery` thêm `flow?: CskhBoardFlow`.

`listLeadCandidates`: khi `query.flow === 're_buyer'`:
- `spaMetaOnly = false` (bỏ clause Meta/agency)
- `clauses.push(buildLeadFlowKindListFilter('re_buyer', 'postgres', 'l'))`

Khi `flow !== 're_buyer'`: giữ nguyên `spa_meta_only !== false`.

Cần `meta_json` trên `crm_leads` trong WHERE — filter util đọc `l.meta_json`. Không đổi SELECT trừ khi thiếu cột (filter không cần SELECT meta).

- [ ] **Step 4: Controller — bỏ 403 cứng `crm_leads` khi BĐS**

`CskhBoardController` **class** đang `@UseGuards(..., StaffLeadsViewGuard)` — user `cskh_lead` chết trước `list()`.

Cách khóa (không nới list SPA):

1. Xóa `StaffLeadsViewGuard` khỏi **class**.
2. Gắn lại `StaffLeadsViewGuard` trên mọi method **trừ** `@Get()` list (và không đụng home-summary — W6).
3. Trong `list()`: `staffAuth.me` → `resolveCskhBoardFlow` → nếu `null` ném `ForbiddenException({ error: 'missing_cap', section: 'crm_leads' })`. Internal key (`staffAuthVia === 'internal'`) = cho qua, flow theo query (default `spa`).

`getBoard({ ...query, flow })`.

- [ ] **Step 5: FE client + rbac + nav**

`fetchCskhBoard` params thêm `flow?: 're_buyer'`. Nếu có: `qs.set('flow', params.flow)`.

`rbac-routes.ts`:

```ts
  {
    prefix: '/crm/cskh-board',
    anyOf: [
      { section: 'crm_board', action: 'view' },
      { section: 'crm_board_kanban', action: 'view' },
      { section: 'crm_board_funnel', action: 'view' },
      { section: 'bds_buyers', action: 'view' },
    ],
  },
  {
    prefix: '/crm/leads',
    anyOf: [
      { section: 'crm_leads', action: 'view' },
      { section: 'bds_buyers', action: 'view' },
    ],
  },
```

`auth.spec.ts` thêm:

```ts
const cskhLead = user([{ section: 'bds_buyers', action: 'view' }]);
expect(canAccessPath('/crm/cskh-board', cskhLead, 'crm')).toBe(true);
expect(canAccessPath('/crm/leads/9', cskhLead, 'crm')).toBe(true);
expect(canAccessPath('/crm/sales', cskhLead, 'crm')).toBe(false);
```

`nav.ts` developer + broker: `bds_buyers.view` → `href: '/crm/cskh-board?flow=re_buyer'`, label giữ «Lead khách mua» / «Lead».

`nav.spec.ts`: assert href chứa `flow=re_buyer`; **không** còn `/crm/bds/leads` là href chính.

`CskhBoardContent`: `const flow = searchParams.get('flow') === 're_buyer' ? 're_buyer' : undefined;` truyền vào `fetchCskhBoard`. Breadcrumb khi `flow`: thêm chip «Khách mua BĐS». **Ẩn** SCI / LMP / script M1 khi `flow === 're_buyer'`.

`/crm/bds/leads` (W1 list): thêm `Link` «Board CSKH» → `/crm/cskh-board?flow=re_buyer`. Nếu `?lead=` có id: `router.replace(`/crm/leads/${lead}`)` (alias unification §5.1).

- [ ] **Step 6: Run tests**

```bash
cd services/ptt-crm-api && ./node_modules/.bin/jest src/cskh-board/cskh-board-flow.util.spec.ts src/leads-funnel/lead-flow-list-filter.util.spec.ts --runInBand
cd services/ops-web && ./node_modules/.bin/vitest run src/lib/auth.spec.ts src/lib/bds/nav.spec.ts
```

Expected: PASS. Default board không `flow` không đổi assertion cũ.

- [ ] **Step 7: Commit** (chỉ khi user yêu cầu)

```bash
git commit -m "$(cat <<'EOF'
feat(bds): filter CSKH board with existing re_buyer flow util

EOF
)"
```

---

### Task 3: Port join + `GET /api/v1/bds/spine/buyer/:leadId`

**Files:**
- Create: `services/ptt-crm-api/src/bds/spine/bds-buyer-query.ts`
- Create: `services/ptt-crm-api/src/bds/spine/bds-buyer-query.spec.ts`
- Create: `services/ptt-crm-api/src/bds/spine/bds-spine-buyer.controller.ts`
- Modify: `services/ptt-crm-api/src/bds/hold/bds-hold.repository.ts` — `latestOpenByLeadIds`
- Modify: `services/ptt-crm-api/src/bds/transactions/bds-tx.repository.ts` — `latestOpenByLeadIds`
- Modify: `services/ptt-crm-api/src/bds/bds.module.ts`
- Modify: `services/ops-web/src/lib/bds/types.ts`
- Modify: `services/ops-web/src/lib/bds/api.ts`
- Modify: `services/ops-web/src/lib/bds/api.spec.ts`

**Interfaces:**
- Consumes: hold/tx/product/lead repos đã có
- Produces: `BdsBuyerQueryService.getByLeadId(leadId, tenantId, opts)` và `getBoardRows(leadIds, tenantId)`

- [ ] **Step 1: Failing test — payload 360**

```ts
import { maskBdsPhone, pickLatestHold, pickLatestTx } from './bds-buyer-query';

describe('bds-buyer-query', () => {
  it('masks phone without view_pii', () => {
    expect(maskBdsPhone('+84912345678', false)).toBe('+849****5678');
    expect(maskBdsPhone('+84912345678', true)).toBe('+84912345678');
  });

  it('prefers active hold over expired', () => {
    const hold = pickLatestHold([
      { id: 'h1', status: 'expired', expires_at: new Date('2026-01-01'), product_id: 1, updated_at: new Date('2026-01-02') },
      { id: 'h2', status: 'active', expires_at: new Date('2026-08-24'), product_id: 2, updated_at: new Date('2026-01-01') },
    ]);
    expect(hold?.id).toBe('h2');
  });

  it('prefers open tx over cancelled', () => {
    const tx = pickLatestTx([
      { id: 't1', stage: 'cancelled', updated_at: new Date('2026-08-01') },
      { id: 't2', stage: 'deposit', updated_at: new Date('2026-07-01') },
    ]);
    expect(tx?.id).toBe('t2');
  });
});
```

Thứ tự hold: `active` > `pending` > khác; cùng rank → `updated_at` mới hơn.  
Thứ tự TX: loại `cancelled`; còn lại `updated_at` mới hơn.

- [ ] **Step 2: Run — expect FAIL**

```bash
cd services/ptt-crm-api && ./node_modules/.bin/jest src/bds/spine/bds-buyer-query.spec.ts --runInBand
```

- [ ] **Step 3: Repo batch + service**

`latestOpenByLeadIds(ids: number[])` trên hold: `WHERE lead_id = ANY($1) AND status IN ('pending','active')`.  
`latestOpenByLeadIds` trên tx: `WHERE lead_id = ANY($1) AND stage <> 'cancelled'`.

`BdsBuyerQueryService.getByLeadId`:

1. Load lead qua `BdsBuyerLeadRepository` (hoặc `crm_leads` theo sqlite id). Nếu không phải `re_buyer` (`resolveLeadFlowKind`) → `NotFoundException` (không lộ SPA).
2. Tenant mismatch → 404.
3. Join hold/tx; unit `unit_code` từ `BdsReProductPgRepository.get` nếu có `product_id`.
4. Visits: `BdsBuyerRepository.listVisits(leadId)` nếu method đã có; không thì `[]`.
5. UTM từ `meta_json`: `utm_source`, `utm_campaign` / `campaign_id`, `ad_id`.
6. Tickets: **không** bắt buộc W5 — `tickets: []` hoặc `getOpenByEntity` nếu inject `@Optional() StaffTicketService`.

Payload:

```ts
export type BdsSpineBuyer = {
  lead_id: number;
  lead_flow_kind: 're_buyer';
  full_name: string;
  phone: string; // masked unless view_pii
  re_project_id: number | null;
  unit_code: string | null;
  product_id: number | null;
  hold: { id: string; status: string; expires_at: string | null } | null;
  tx: { id: string; stage: string } | null;
  visits: Array<{ scheduled_at: string; outcome: string }>;
  utm: { source: string; campaign_id: string; ad_id: string };
  touched_at: string | null;
};
```

`getBoardRows(leadIds)`: cùng join, **không** visits/utm (nhẹ). Return `Map<number, { re_project_id, unit_code, hold_expires_at, tx_stage }>`.

- [ ] **Step 4: HTTP**

`bds-spine-buyer.controller.ts`:

```ts
@Controller('api/v1/bds/spine')
@UseGuards(StaffOrInternalKeyGuard, BdsPackGuard, BdsBuyerGuard)
export class BdsSpineBuyerController {
  @Get('buyer/:leadId')
  getBuyer(
    @Param('leadId', ParseIntPipe) leadId: number,
    @Headers('x-bds-tenant') tenantId?: string,
    @Req() req?: StaffReq,
  ) { /* view_pii from staffAuth.hasCap(..., 'bds_buyers', 'view_pii') */ }
}
```

Đăng ký controller + `BdsBuyerQueryService` trong `BdsModule`; **export** `BdsBuyerQueryService`.

- [ ] **Step 5: FE client**

```ts
export function fetchBdsSpineBuyer(token: string, leadId: number) {
  return bdsFetch<BdsSpineBuyer>(token, `/api/v1/bds/spine/buyer/${leadId}`);
}
```

`api.spec.ts`: path chứa `/api/v1/bds/spine/buyer/9`.

- [ ] **Step 6: Run**

```bash
cd services/ptt-crm-api && ./node_modules/.bin/jest src/bds/spine/bds-buyer-query.spec.ts --runInBand
cd services/ops-web && ./node_modules/.bin/vitest run src/lib/bds/api.spec.ts
```

- [ ] **Step 7: Commit** (chỉ khi user yêu cầu)

```bash
git commit -m "$(cat <<'EOF'
feat(bds): add spine buyer join for CSKH 360

EOF
)"
```

---

### Task 4: Enrich board + cột căn / TTL / TX

**Files:**
- Modify: `services/ptt-crm-api/src/cskh-board/cskh-board.module.ts` — `forwardRef(() => BdsModule)`
- Modify: `services/ptt-crm-api/src/cskh-board/cskh-board.service.ts`
- Modify: `services/ptt-crm-api/src/cskh-board/cskh-board.types.ts` — field optional trên `CskhBoardRow`
- Modify: `services/ops-web/src/lib/api.ts` — `CskhBoardRow` optional fields
- Modify: `services/ops-web/src/app/crm/cskh-board/CskhBoardContent.tsx`
- Create: `services/ops-web/src/lib/bds/cskh-board-re-buyer.ts`
- Create: `services/ops-web/src/lib/bds/cskh-board-re-buyer.spec.ts`

**Interfaces:**
- Consumes: `BdsBuyerQueryService.getBoardRows`, `flow` từ Task 2
- Produces: row có `unit_code`, `hold_expires_at`, `tx_stage`, `re_project_id` khi `flow=re_buyer`

- [ ] **Step 1: Failing FE test — cột khi nào hiện**

```ts
import { showReBuyerBoardColumns } from './cskh-board-re-buyer';

describe('showReBuyerBoardColumns', () => {
  it('true only for flow=re_buyer', () => {
    expect(showReBuyerBoardColumns('re_buyer')).toBe(true);
    expect(showReBuyerBoardColumns(undefined)).toBe(false);
  });
});
```

- [ ] **Step 2: Run — FAIL**

```bash
cd services/ops-web && ./node_modules/.bin/vitest run src/lib/bds/cskh-board-re-buyer.spec.ts
```

- [ ] **Step 3: Enrich Nest**

`CskhBoardRow` thêm optional:

```ts
  re_project_id?: number | null;
  unit_code?: string | null;
  hold_expires_at?: string | null;
  tx_stage?: string | null;
```

Trong `getBoard`, sau `enriched` map, nếu `query.flow === 're_buyer'` và `buyerQuery` có:

```ts
const extra = await this.buyerQuery.getBoardRows(enriched.map((r) => r.id), tenantId);
```

Gắn field. `@Optional()` — pack tắt thì cột null, board vẫn list lead.

Default `spa` **không** gọi port (không N+1).

- [ ] **Step 4: FE columns**

`showReBuyerBoardColumns(flow) => flow === 're_buyer'`.

Khi true, thêm `<th>`: Dự án · Căn · Hold TTL · TX. Card mobile: cùng 4 dòng.  
TTL: nếu `hold_expires_at` < now → text đỏ.  
SLA: ưu tiên pill `first_call_15m`; badge đỏ khi `sla_state === 'breach'` (đã có `cskh-board-card--breach`).  
Link tên: `/crm/leads/${row.id}`.

Ẩn cột B2 done / SCI khi `flow=re_buyer`.

- [ ] **Step 5: Run**

```bash
cd services/ops-web && ./node_modules/.bin/vitest run src/lib/bds/cskh-board-re-buyer.spec.ts
```

- [ ] **Step 6: Commit** (chỉ khi user yêu cầu)

```bash
git commit -m "$(cat <<'EOF'
feat(bds): show unit hold and tx columns on re_buyer board

EOF
)"
```

---

### Task 5: Panel 360 người mua

**Files:**
- Create: `services/ops-web/src/lib/bds/BdsBuyer360Panel.tsx`
- Modify: `services/ops-web/src/app/crm/leads/[id]/page.tsx`
- Modify: `services/ops-web/src/app/crm/bds/leads/page.tsx` (redirect `?lead=` nếu chưa làm Task 2)

**Interfaces:**
- Consumes: `fetchBdsSpineBuyer`, `shouldHideDealRoom`, `leadFlowKind`
- Produces: panel tabs tối thiểu: Ads · Visit · Hold · TX (link Thu/Sổ)

- [ ] **Step 1: Mount condition (document in panel file)**

Hiện panel khi `leadFlowKind === 're_buyer'` **hoặc** `shouldHideDealRoom({ leadFlowKind, user })` và user có `bds_buyers.view`.  
`fetchLead` 403: vẫn mount panel nếu `bds_buyers.view` — spine là nguồn.

- [ ] **Step 2: Panel UI (không test RTL bắt buộc — logic path ở api.spec đã có)**

Copy giọng `HubPageLayout` / aftersales: `page-card`, tiếng Việt.

```tsx
export function BdsBuyer360Panel(props: { token: string; leadId: number; canViewPii: boolean }) {
  // useEffect fetchBdsSpineBuyer
  // Banner: Khách mua BĐS — không Deal Room
  // Hàng: UTM · chạm · căn/TTL · stage TX
  // Link: /crm/bds/holds?hold=  /crm/bds/transactions?tx=  /crm/bds/aftersales?tx= (khi contracted+)
  // Link Việc: /crm/work nếu tickets FE on
}
```

Không tab Proposal. Không fetch Deal Room.

Trên `leads/[id]`, khi `re_buyer`: ẩn `LeadPresalesFunnelStepper`, banner SPA «Luồng CSKH vận hành 24h», consult/LMP subtabs.

Banner thay:

```tsx
{leadFlowKind === 're_buyer' ? (
  <div className="banner banner-info">
    <strong>Khách mua BĐS</strong>
    <p>Ads → chạm 15p → xem nhà → hold → cọc. Không Deal Room agency.</p>
  </div>
) : /* existing spa / b2b banners */}
```

- [ ] **Step 3: FE tests không vỡ**

```bash
cd services/ops-web && ./node_modules/.bin/vitest run src/lib/crm/lead-flow-kind.spec.ts src/lib/bds/deal-room-hide.spec.ts src/lib/bds/api.spec.ts
```

Expected: PASS.

- [ ] **Step 4: Commit** (chỉ khi user yêu cầu)

```bash
git commit -m "$(cat <<'EOF'
feat(bds): add buyer 360 panel on lead detail

EOF
)"
```

---

### Task 6: After intake + verify U-02 / U-03 / không W6+

**Files:**
- Modify: `services/ptt-crm-api/src/bds/aftersales/bds-aftersales.service.ts`
- Modify: `services/ptt-crm-api/src/bds/aftersales/bds-aftersales.service.spec.ts`
- Modify: `services/ptt-crm-api/src/bds/aftersales/bds-aftersales.repository.ts` — `seedChecksIfEmpty` nếu chưa có
- Modify: `services/ptt-crm-api/src/bds/transactions/bds-tx.service.ts` — sau `setStageIf` contracted
- Modify: `services/ptt-crm-api/src/bds/transactions/bds-tx.service.spec.ts`
- Modify: `services/ptt-crm-api/src/bds/bds.module.ts` — `BdsTxService` đã có after? inject `@Optional() BdsAftersalesService` **cẩn thận vòng** — `ensureIntake` nên là method trên aftersales; tx đã inject nhiều service. Nếu vòng: gọi `asRepo.seedChecksIfEmpty` từ tx **cấm** (lệch layer). Ưu tiên: `BdsAftersalesService.ensureIntake(tx)` + Nest forwardRef nếu cần.

**Interfaces:**
- Consumes: `TxRow` sau contract
- Produces: `ensureIntake(tx): Promise<void>` idempotent; 4 row `bds_handover_checks` nếu trống

- [ ] **Step 1: Failing test after**

```ts
  it('AF-01 ensureIntake seeds 4 checks once', async () => {
    asRepo.listChecks.mockResolvedValue([]);
    asRepo.seedChecksIfEmpty.mockResolvedValue(undefined);
    await svc.ensureIntake(tx({ stage: 'contracted' }));
    expect(asRepo.seedChecksIfEmpty).toHaveBeenCalledWith('tx1', expect.anything());
    await svc.ensureIntake(tx({ stage: 'contracted' }));
    expect(asRepo.seedChecksIfEmpty).toHaveBeenCalledTimes(2); // repo no-ops if exist
  });

  it('ensureIntake no-op when not contracted', async () => {
    await svc.ensureIntake(tx({ stage: 'deposit' }));
    expect(asRepo.seedChecksIfEmpty).not.toHaveBeenCalled();
  });
```

TX spec: `contract()` gọi `aftersales.ensureIntake` 1 lần (mock).

- [ ] **Step 2: Run — FAIL**

```bash
cd services/ptt-crm-api && ./node_modules/.bin/jest src/bds/aftersales/bds-aftersales.service.spec.ts src/bds/transactions/bds-tx.service.spec.ts --runInBand
```

- [ ] **Step 3: Implement**

`seedChecksIfEmpty(txId, tenantId)`: lặp `HANDOVER_CHECK_CODES` = `['water', 'electric', 'interior', 'minutes']` (`bds-aftersales.types.ts`). `INSERT ... ON CONFLICT (transaction_id, item_code) DO NOTHING`. Status mặc định không `'pass'`.

`ensureIntake`: `if (tx.stage !== 'contracted') return; await this.asRepo.seedChecksIfEmpty(...)`.

`contract()` sau commission/capi/tickets:

```ts
    try {
      await this.aftersales?.ensureIntake(updated);
    } catch (err) {
      this.logger.warn(`after intake tx=${updated.id}: ${String(err)}`);
    }
```

Không set `handover_appointment_at`. Không staff ticket after (BR-BDS-46).

- [ ] **Step 4: Run after + tx + deal-room 404**

```bash
cd services/ptt-crm-api && ./node_modules/.bin/jest \
  src/bds/aftersales/bds-aftersales.service.spec.ts \
  src/bds/transactions/bds-tx.service.spec.ts \
  src/deal-room/deal-room.service.spec.ts \
  src/cskh-board/cskh-board-flow.util.spec.ts \
  src/bds/spine/bds-buyer-query.spec.ts \
  --runInBand
```

```bash
cd services/ops-web && ./node_modules/.bin/vitest run src/lib/bds src/lib/crm/lead-flow-kind.spec.ts src/lib/auth.spec.ts
```

- [ ] **Step 5: Leak check W6+**

Không `PTT_BDS_NAV_HIDE_B2B`, không sửa `OpsNav` `/crm/sales`, không `bds-hub` widget mới, không CAPI value, không `offboard`, không `home-summary` block `re_buyer`.

```bash
rg -n "PTT_BDS_NAV_HIDE_B2B|finance/hub|offboard|PTT_BDS_CSKH_BOARD|PTT_BDS_OS" \
  services/ptt-crm-api/src/cskh-board services/ptt-crm-api/src/bds/spine \
  services/ops-web/src/app/crm/cskh-board services/ops-web/src/lib/bds \
  services/ops-web/src/app/crm/leads
```

Expected: không hit flag/nav/finance/offboard mới (trừ comment plan).

- [ ] **Step 6: Commit** (chỉ khi user yêu cầu)

```bash
git commit -m "$(cat <<'EOF'
feat(bds): seed aftersales intake when transaction contracts

EOF
)"
```

---

## Coverage vs spec §7 W5

| Tiêu chí | Task |
|----------|------|
| Board `re_buyer` 15p | 2, 4 |
| 0 lead SPA trên `flow=re_buyer` (U-02) | 2 |
| Cột căn / hold / TX | 3, 4 |
| Deal Room 404 + ẩn 360 (U-03, CS-06) | 1, 5 |
| Người mua 360 1 URL (U1, CS-01) | 3, 5 |
| `cskh_lead` vào được nhà (cap) | 2 |
| After auto row (AF-01) | 6 |
| Deep-link Việc (CS-02) | 4–5 link `/crm/work` — ticket W4 đã tạo |
| Qualify/touch/visit (CS-03–05) | **W1 list** giữ `/crm/bds/leads` + link từ board |
| Không W6+ | 6 leak check |

## UAT staging (sau deploy Nest + ops-web)

| Việc | Pass |
|------|------|
| Login `cskh_lead` | Nav «Lead khách mua» → `/crm/cskh-board?flow=re_buyer` |
| Board | Chỉ `re_buyer`; có cột căn/TTL/TX; SLA 15p đỏ nếu chưa chạm |
| User PTT `crm_leads.view` mở `/crm/cskh-board` **không** `flow` | Vẫn board SPA Meta — không mất lead agency |
| Mở 360 từ card | Không banner / link Deal Room; có Ads/hold/TX |
| `/crm/leads/:id/deal-room` `re_buyer` | 404 «Không tìm thấy» |
| Ký HĐMB | `/crm/bds/aftersales` có đúng 1 hàng TX; checklist 0/4; F5 không nhân hàng |
| Replay `contract` / refresh | Vẫn 4 check, không duplicate |

Deploy W5: **cả** `realosai-api` (Nest dist) **và** `realosai-ops-web` (standalone). Không DDL mới.

## Escalate

| Gap | Hướng |
|-----|--------|
| `crm_leads.meta_json` thiếu trên PG list | Filter util đã đọc `meta_json` — nếu cột null, `re_project_id` trên cột riêng: **không** invent cột; escalate |
| Vòng Nest `BdsModule` ↔ `CskhBoardModule` | `forwardRef`; nếu vẫn vòng: enrich board bằng SQL thuần trong `cskh-board.repository` (JOIN hold/tx) — **không** HTTP nội bộ |
| `StaffLeadsViewGuard` class-level khó gỡ | Guard thay `canActivate`: `crm_leads.view` OR (`bds_buyers.view` AND path là GET list board). Không nới export.xlsx |
| Unit `unit_code` null | Hiện `product_id` — đủ U-02 |

---

*Sau khi duyệt plan: cập nhật Task 14–15 trong [2026-08-23-bds-os-coding.md](./2026-08-23-bds-os-coding.md).*
