# P10 Triển khai — Ra quân / Launch (TTL 180s, queue, war-room)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sự kiện ra quân trên PG: tạo/mở/đóng `bds_launches`, khi `open` hold mới TTL = `hold_ttl_seconds` (seed 180 — BDS-36 / BR-BDS-33), khóa `price_list_id`, hàng đợi FIFO `bds_unit_queues` + promote khi TTL hết (UC-046), war-room poll 3 cột, đóng launch → TTL về CSBH + hoàn giữ chỗ chưa cọc (BDS-37). UI mỏng `/crm/bds/launches` (SCR-BDS-070).

**Architecture:** Bounded context `src/bds/launches/` (`BdsLaunchService` = spec §23.5). HTTP sau `StaffOrInternalKeyGuard` + `BdsPackGuard` + `BdsLaunchGuard` (`PTT_BDS_PACK=1` **và** `PTT_BDS_LAUNCH=1`). Hook hold: `BdsHoldService.create` đọc `BdsLaunchRepository.getOpenByProject` **chỉ khi** LAUNCH=1 — flag tắt → hold P2 nguyên (TTL phút CSBH). Promote: `expireDue` gọi `launch.promoteNext(productId)` sau khi hết hold. Tenant `broker` → war-room / list CĐT **404**; sàn vẫn hold được với TTL 180s nếu launch `open`. Không huddle chat (P11). Không SSE (poll 3s). Không import `ReProjectsModule`.

**Tech Stack:** NestJS `ptt-crm-api` + Jest; Next.js `ops-web` + Vitest; `pg` Pool; `psql`.

**Spec:** [2026-08-21-bds-industry-pack-design.md](../specs/2026-08-21-bds-industry-pack-design.md) §10.4, §15 P10, §23.5, BR-BDS-33/30.  
**UX:** [2026-08-22-bds-ux-ui-design.md](../specs/2026-08-22-bds-ux-ui-design.md) §4.10 SCR-BDS-070.  
**UC:** [13-BDS-INDUSTRY-PACK.md](../../use-cases/13-BDS-INDUSTRY-PACK.md) UC-045, UC-046, UC-016 E (BDS-37), UC-017 E1.  
**P2:** [2026-08-22-bds-p2-hold-ttl.md](./2026-08-22-bds-p2-hold-ttl.md) — hold + `expireDue` + BDS-02/03.  
**P3:** [2026-08-22-bds-p3-csbh.md](./2026-08-22-bds-p3-csbh.md) — `price_list_id` / `hold_ttl_minutes` trên phase + CSBH.  
**P1b:** [2026-08-22-bds-p1b-project-os.md](./2026-08-22-bds-p1b-project-os.md) — `bds_launch_phases`.  
**P4:** [2026-08-22-bds-p4-transaction.md](./2026-08-22-bds-p4-transaction.md) — TX `reservation` (phí giữ chỗ).  
**P8:** [2026-08-22-bds-p8-ui-rbac.md](./2026-08-22-bds-p8-ui-rbac.md) — cap `bds_launches` đã catalog; nav **chưa** có «Ra quân».  
**P9:** [2026-08-22-bds-p9-aftersales.md](./2026-08-22-bds-p9-aftersales.md) — không đụng.  
**Roadmap:** [2026-08-22-bds-coding-roadmap.md](./2026-08-22-bds-coding-roadmap.md)

**Test ID P10:** BDS-36 (launch open → hold `expires_at` khớp 180s).  
**UC-045** open khóa giá + TTL ngắn; close → TTL CSBH.  
**UC-046** hai POST cùng căn = 201 + 409; 409 enqueue FIFO; TTL hết → promote người kế.  
**BDS-37** đóng cửa sổ: TX `reservation` chưa cọc → `cancelled` `lost_reason=launch_window`, căn `available`.  
**BDS-02** trên launch = cùng 409 `unit_locked` (không đổi mã lỗi).  
**Huddle `launch_*` / SSE / ticket `ops_action`** = **P11/P12**.  
**UC-047** không có trong UC file — **không** bịa màn.  
**Checklist pre-open đầy đủ (giỏ / kit MKT)** = ngoài v1 (open không chặn thiếu kit).

**Hướng khóa: 1** — API đủ cổng + hook hold TTL + queue/promote + war-room poll + page mỏng. Không SSE, không chat, không wizard.

| Hướng | Làm | Không | Khi nào chọn |
|-------|-----|-------|----------------|
| **1 (khóa)** | Flag + DDL + CRUD launch + TTL 180s + queue FIFO + BDS-37 close + page `/crm/bds/launches` | SSE, huddle, checklist kit | Đúng cổng P2+P3→P10 |
| 2 | Chỉ API, không FE | Nav «Ra quân» | Tách UI sang P10b |
| 3 | War-room realtime SSE + huddle + kit MKT | — | Quá rộng; SSE/chat = P11 |

---

## Global Constraints

- `PTT_BDS_PACK` mặc định `0` — mọi `/api/v1/bds/*` = **404**.
- `PTT_BDS_LAUNCH` mặc định `0` — route launch + hook TTL 180s + enqueue = **404 / no-op**. Hold P2 **nguyên**.
- GET ngoài tenant = **404**, không 403, không PII (BR-BDS-05).
- Tenant `broker`: `GET /launches` + war-room **404**. Hold sàn vẫn tạo được (TTL 180s nếu launch open trên cùng `project_id`).
- Một dự án **một** launch `open` (unique partial).
- Launch `open`: `expires_at` hold **active** mới = `now + hold_ttl_seconds` (seed 180). Pending kênh không set expires (duyệt P2 rồi mới TTL).
- Hai POST hold cùng căn = 201 + 409 `{ error: 'unit_locked' }` (BDS-02). Khi LAUNCH=1 và launch `open`: request 409 **còn** ghi queue `waiting`.
- Đóng launch: hold **mới** dùng TTL CSBH (`ttlMinutes` P2). Hold đang mở **giữ** `expires_at` cũ (không kéo dài).
- BDS-37: không ghi sổ cái / không chuyển khoản — chỉ hủy TX + căn available + `lost_reason=launch_window`.
- `BdsModule` **không** import `ReProjectsModule`.
- Hook hold: inject `@Optional() BdsLaunchRepository` (không inject `BdsLaunchService` — tránh vòng). Promote: `BdsLaunchService` gọi `BdsHoldService.create`.
- Tiếng Việt UI: Ra quân · Hàng đợi · Xung đột. Không «Launch / War room» trên label nút (subtitle được ghi SCR).
- `DATABASE_URL` script: `postgresql://ptt:ptt_dev@127.0.0.1:5433/rnosaidb`.
- Test API: `cd services/ptt-crm-api && ./node_modules/.bin/jest <file> --runInBand`.
- Test FE: `cd services/ops-web && ./node_modules/.bin/vitest run <file>`.
- Không commit trừ khi user yêu cầu.
- Prod VPS: **không** bật `PTT_BDS_PACK` / `PTT_BDS_LAUNCH`.

---

## 0. Phạm vi / ngoài phạm vi

**Làm**

- Flag `isBdsLaunchEnabled()` + `BdsLaunchGuard`
- DDL `bds_launches`, `bds_unit_queues`
- `POST /projects/:id/launches` (draft)
- `POST /launches/:id/open` (khóa giá, status `open`)
- `POST /launches/:id/close` (TTL về CSBH + BDS-37)
- `GET /launches` · `GET /launches/:id` · `GET /launches/:id/war-room`
- Hook `BdsHoldService.create`: TTL 180s khi có launch `open` cùng `project_id`
- Enqueue khi 409 trong cửa sổ launch
- `expireDue` → `promoteNext` (tạo hold cho `waiting` cũ nhất cùng `product_id`)
- Page + nav «Ra quân» (cap `bds_launches` view); ẩn broker
- Poll 3s trên war-room khi `status=open`

**Không làm**

- Huddle chat `launch_*` / archive room (P11)
- SSE / WebSocket
- Ticket `ops_action` war-room (P12)
- Checklist pre-open bắt buộc (giỏ / kit MKT / pháp lý) — open **không** 400 vì thiếu kit
- Đổi BDS-02 mã lỗi
- Kéo `expires_at` hold đang mở khi close
- Graph / CAPI
- Payroll / hoàn tiền thật

---

## 1. File map

```
docs/specs/postgresql-ddl-bds-p10.sql
scripts/apply_pg_ddl_bds_p10.sh

services/ptt-crm-api/src/bds/bds.flags.ts                         # + isBdsLaunchEnabled
services/ptt-crm-api/src/bds/bds.flags.spec.ts
services/ptt-crm-api/src/config/app-config.service.ts             # bdsLaunchEnabled
services/ptt-crm-api/src/bds/guards/bds-launch.guard.ts
services/ptt-crm-api/src/bds/guards/bds-launch.guard.spec.ts
services/ptt-crm-api/src/bds/launches/bds-launch.types.ts
services/ptt-crm-api/src/bds/launches/bds-launch.util.ts
services/ptt-crm-api/src/bds/launches/bds-launch.util.spec.ts
services/ptt-crm-api/src/bds/launches/bds-launch.repository.ts
services/ptt-crm-api/src/bds/launches/bds-launch.service.ts
services/ptt-crm-api/src/bds/launches/bds-launch.service.spec.ts
services/ptt-crm-api/src/bds/launches/bds-launch.controller.ts
services/ptt-crm-api/src/bds/launches/bds-launch.controller.spec.ts
services/ptt-crm-api/src/bds/hold/bds-hold.service.ts             # TTL + enqueue + promote hook
services/ptt-crm-api/src/bds/hold/bds-hold.service.spec.ts        # BDS-36
services/ptt-crm-api/src/bds/bds.module.ts

services/ops-web/src/lib/bds/api.ts
services/ops-web/src/lib/bds/nav.ts
services/ops-web/src/lib/bds/nav.spec.ts
services/ops-web/src/components/layout/nav-icons.tsx
services/ops-web/src/app/crm/bds/launches/page.tsx

docs/superpowers/plans/2026-08-22-bds-coding-roadmap.md
```

Không sửa DDL P0–P9 (không ALTER `bds_holds`). Queue + launch là bảng mới.

---

### Task 1: Flag LAUNCH + util TTL giây / queue FIFO

**Files:**
- Modify: `services/ptt-crm-api/src/bds/bds.flags.ts`
- Modify: `services/ptt-crm-api/src/bds/bds.flags.spec.ts`
- Modify: `services/ptt-crm-api/src/config/app-config.service.ts`
- Create: `services/ptt-crm-api/src/bds/guards/bds-launch.guard.ts`
- Create: `services/ptt-crm-api/src/bds/guards/bds-launch.guard.spec.ts`
- Create: `services/ptt-crm-api/src/bds/launches/bds-launch.types.ts`
- Create: `services/ptt-crm-api/src/bds/launches/bds-launch.util.ts`
- Create: `services/ptt-crm-api/src/bds/launches/bds-launch.util.spec.ts`

**Interfaces:**
- Consumes: `envFlagOn`
- Produces: `isBdsLaunchEnabled()`, `DEFAULT_HOLD_TTL_SECONDS`, `computeLaunchExpiresAt()`, `canOpenLaunch()`, `canCloseLaunch()`

- [ ] **Step 1: Write failing tests**

`bds.flags.spec.ts` — restore `PTT_BDS_LAUNCH` trong `afterEach`:

```ts
it('defaults LAUNCH off when unset', () => {
  delete process.env.PTT_BDS_LAUNCH;
  expect(isBdsLaunchEnabled()).toBe(false);
});

it('LAUNCH on for 1', () => {
  process.env.PTT_BDS_LAUNCH = '1';
  expect(isBdsLaunchEnabled()).toBe(true);
});
```

`bds-launch.util.spec.ts`:

```ts
import {
  DEFAULT_HOLD_TTL_SECONDS,
  canCloseLaunch,
  canOpenLaunch,
  computeLaunchExpiresAt,
} from './bds-launch.util';

describe('bds-launch.util', () => {
  it('BDS-36: default TTL is 180s', () => {
    expect(DEFAULT_HOLD_TTL_SECONDS).toBe(180);
  });

  it('expires_at = now + ttl seconds', () => {
    const now = new Date('2026-08-22T10:00:00Z');
    expect(computeLaunchExpiresAt(now, 180).toISOString()).toBe('2026-08-22T10:03:00.000Z');
  });

  it('open only from draft', () => {
    expect(canOpenLaunch('draft')).toBe(true);
    expect(canOpenLaunch('open')).toBe(false);
    expect(canOpenLaunch('closed')).toBe(false);
  });

  it('close only from open', () => {
    expect(canCloseLaunch('open')).toBe(true);
    expect(canCloseLaunch('draft')).toBe(false);
    expect(canCloseLaunch('closed')).toBe(false);
  });
});
```

`bds-launch.guard.spec.ts` — copy `bds-aftersales.guard.spec.ts`, đổi `AFTERSALES` → `LAUNCH`.

- [ ] **Step 2: Run — expect FAIL**

Run: `cd services/ptt-crm-api && ./node_modules/.bin/jest src/bds/bds.flags.spec.ts src/bds/launches/bds-launch.util.spec.ts src/bds/guards/bds-launch.guard.spec.ts --runInBand`

Expected: FAIL — `isBdsLaunchEnabled` / files missing.

- [ ] **Step 3: Minimal implementation**

```ts
export function isBdsLaunchEnabled(): boolean {
  return envFlagOn(process.env.PTT_BDS_LAUNCH);
}
```

`app-config.service.ts` cạnh `bdsAftersalesEnabled`:

```ts
readonly bdsLaunchEnabled: boolean;
this.bdsLaunchEnabled = ['1', 'true', 'yes', 'on'].includes(
  (process.env.PTT_BDS_LAUNCH ?? '0').trim().toLowerCase(),
);
```

`bds-launch.guard.ts` — PACK off **hoặc** LAUNCH off → `NotFoundException`.

`bds-launch.types.ts`:

```ts
export const DEFAULT_HOLD_TTL_SECONDS = 180;

export type LaunchStatus = 'draft' | 'open' | 'closed';
export type QueueStatus = 'waiting' | 'promoted' | 'cancelled';

export type LaunchRow = {
  id: string;
  tenant_id: string | null;
  project_id: number;
  phase_id: string | null;
  starts_at: Date | null;
  ends_at: Date | null;
  hold_ttl_seconds: number;
  price_list_id: number | null;
  status: LaunchStatus;
  opened_at: Date | null;
  closed_at: Date | null;
  created_at: Date;
};

export type QueueRow = {
  id: string;
  tenant_id: string | null;
  launch_id: string;
  product_id: number;
  lead_id: number;
  requested_by_staff_id: number | null;
  channel_partner_id: string;
  status: QueueStatus;
  created_at: Date;
};

export type WarRoomHold = {
  hold_id: string;
  product_id: number;
  lead_id: number;
  status: string;
  expires_at: Date | null;
  ttl_remaining_sec: number | null;
};

export type WarRoomResponse = {
  launch: LaunchRow;
  holds: WarRoomHold[];
  queues: QueueRow[];
  conflicts: Array<{ product_id: number; waiting: number }>;
};
```

`bds-launch.util.ts`:

```ts
import { DEFAULT_HOLD_TTL_SECONDS, type LaunchStatus } from './bds-launch.types';

export { DEFAULT_HOLD_TTL_SECONDS };

export function computeLaunchExpiresAt(now: Date, ttlSeconds: number): Date {
  const sec = Number.isFinite(ttlSeconds) && ttlSeconds > 0 ? ttlSeconds : DEFAULT_HOLD_TTL_SECONDS;
  return new Date(now.getTime() + sec * 1000);
}

export function canOpenLaunch(status: string): boolean {
  return status === 'draft';
}

export function canCloseLaunch(status: string): boolean {
  return status === 'open';
}

export function ttlRemainingSec(expiresAt: Date | null, now = new Date()): number | null {
  if (!expiresAt) return null;
  return Math.max(0, Math.round((expiresAt.getTime() - now.getTime()) / 1000));
}
```

- [ ] **Step 4: Run — expect PASS**

- [ ] **Step 5: Commit** — chỉ khi user yêu cầu.

---

### Task 2: DDL P10 + apply script

**Files:**
- Create: `docs/specs/postgresql-ddl-bds-p10.sql`
- Create: `scripts/apply_pg_ddl_bds_p10.sh`

**Interfaces:**
- Consumes: `bds_launch_phases` (P1b), `bds_tenants`, `crm_re_projects`
- Produces: `bds_launches`, `bds_unit_queues`

- [ ] **Step 1: Write SQL**

```sql
-- Pack BĐS P10 — Apply: scripts/apply_pg_ddl_bds_p10.sh
BEGIN;

CREATE TABLE IF NOT EXISTS bds_launches (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES bds_tenants (id),
  project_id INTEGER NOT NULL REFERENCES crm_re_projects (id) ON DELETE CASCADE,
  phase_id UUID REFERENCES bds_launch_phases (id),
  starts_at TIMESTAMPTZ,
  ends_at TIMESTAMPTZ,
  hold_ttl_seconds INTEGER NOT NULL DEFAULT 180
    CHECK (hold_ttl_seconds > 0 AND hold_ttl_seconds <= 86400),
  price_list_id INTEGER,
  status TEXT NOT NULL DEFAULT 'draft'
    CHECK (status IN ('draft', 'open', 'closed')),
  opened_at TIMESTAMPTZ,
  closed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE UNIQUE INDEX IF NOT EXISTS uq_bds_launches_one_open
  ON bds_launches (project_id)
  WHERE status = 'open';

CREATE INDEX IF NOT EXISTS idx_bds_launches_tenant_status
  ON bds_launches (tenant_id, status);

CREATE TABLE IF NOT EXISTS bds_unit_queues (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id UUID REFERENCES bds_tenants (id),
  launch_id UUID NOT NULL REFERENCES bds_launches (id) ON DELETE CASCADE,
  product_id INTEGER NOT NULL REFERENCES crm_re_project_products (id) ON DELETE CASCADE,
  lead_id INTEGER NOT NULL,
  requested_by_staff_id INTEGER,
  channel_partner_id TEXT NOT NULL DEFAULT '',
  status TEXT NOT NULL DEFAULT 'waiting'
    CHECK (status IN ('waiting', 'promoted', 'cancelled')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE UNIQUE INDEX IF NOT EXISTS uq_bds_unit_queues_waiting
  ON bds_unit_queues (launch_id, product_id, lead_id)
  WHERE status = 'waiting';

CREATE INDEX IF NOT EXISTS idx_bds_unit_queues_fifo
  ON bds_unit_queues (launch_id, product_id, created_at)
  WHERE status = 'waiting';

COMMIT;
```

`scripts/apply_pg_ddl_bds_p10.sh` — copy P9, đổi `p9` → `p10`.

- [ ] **Step 2: Apply local nếu PG :5433 chạy**

Run: `DATABASE_URL=postgresql://ptt:ptt_dev@127.0.0.1:5433/rnosaidb bash scripts/apply_pg_ddl_bds_p10.sh`

Expected: `OK  bds P10 DDL`. Nếu PG tắt: không chặn Task 3.

- [ ] **Step 3: Commit** — chỉ khi user yêu cầu.

---

### Task 3: Repository

**Files:**
- Create: `services/ptt-crm-api/src/bds/launches/bds-launch.repository.ts`

**Interfaces:**
- Produces:

```ts
insert(input: {
  tenant_id: string | null;
  project_id: number;
  phase_id: string | null;
  starts_at: Date | null;
  ends_at: Date | null;
  hold_ttl_seconds: number;
  price_list_id: number | null;
}): Promise<LaunchRow>

getById(id: string): Promise<LaunchRow | null>
getOpenByProject(projectId: number): Promise<LaunchRow | null>
listByTenant(tenantId: string, projectId?: number): Promise<LaunchRow[]>
setStatusIf(id: string, status: LaunchStatus, extra: Record<string, unknown>, expected: LaunchStatus): Promise<LaunchRow | null>

enqueue(input: {
  tenant_id: string | null;
  launch_id: string;
  product_id: number;
  lead_id: number;
  requested_by_staff_id: number | null;
  channel_partner_id: string;
}): Promise<QueueRow>
peekWaiting(launchId: string, productId: number): Promise<QueueRow | null>
setQueueStatusIf(id: string, status: QueueStatus, expected: QueueStatus): Promise<QueueRow | null>
listWaiting(launchId: string): Promise<QueueRow[]>
countWaitingByProduct(launchId: string): Promise<Array<{ product_id: number; waiting: number }>>
listActiveHoldsForProject(projectId: number): Promise<Array<{
  id: string; product_id: number; lead_id: number; status: string; expires_at: Date | null;
}>>
```

`enqueue` gặp unique `waiting` cùng lead+căn → trả row hiện có (không 500). `peekWaiting` = `ORDER BY created_at ASC LIMIT 1`.

`listActiveHoldsForProject` đọc `bds_holds` `status IN ('active','pending')` — không thêm cột `launch_id`.

- [ ] **Step 1:** Không bắt buộc test repo (mock ở service).

- [ ] **Step 2: Commit** — chỉ khi user yêu cầu.

---

### Task 4: Service — create / open / close + BDS-37

**Files:**
- Create: `services/ptt-crm-api/src/bds/launches/bds-launch.service.ts`
- Create: `services/ptt-crm-api/src/bds/launches/bds-launch.service.spec.ts`

**Interfaces:**
- Consumes: `BdsLaunchRepository`, `BdsTenantService.getMe`, `BdsProjectOsService.getPhase` (optional), `@Optional() BdsTxService` (close refund), `@Optional() BdsHoldService` (promote)
- Produces: `create`, `open`, `close`, `list`, `get`, `warRoom`, `enqueueOnConflict`, `promoteNext`

Cách chặn broker: `getMe(tenantId)` — `mode === 'broker'` → 404 trên `list` / `get` / `warRoom` / `create` / `open` / `close`. `enqueueOnConflict` / `promoteNext` **không** check broker (gọi từ hold).

- [ ] **Step 1: Write failing service tests**

```ts
import { BadRequestException, ConflictException, NotFoundException } from '@nestjs/common';
import { BdsLaunchService } from './bds-launch.service';

function launch(over: Record<string, unknown> = {}) {
  return {
    id: 'L1',
    tenant_id: 't1',
    project_id: 7,
    phase_id: 'ph1',
    hold_ttl_seconds: 180,
    price_list_id: 3,
    status: 'draft',
    ...over,
  };
}

describe('BdsLaunchService', () => {
  const repo = {
    insert: jest.fn(),
    getById: jest.fn(),
    getOpenByProject: jest.fn(),
    listByTenant: jest.fn(),
    setStatusIf: jest.fn(),
    enqueue: jest.fn(),
    peekWaiting: jest.fn(),
    setQueueStatusIf: jest.fn(),
    listWaiting: jest.fn(),
    countWaitingByProduct: jest.fn(),
    listActiveHoldsForProject: jest.fn(),
  };
  const tenants = { getMe: jest.fn().mockResolvedValue({ mode: 'developer' }) };
  const projectOs = { getPhase: jest.fn().mockResolvedValue({ id: 'ph1', project_id: 7, price_list_id: 3 }) };
  const txs = { cancelLaunchReservations: jest.fn().mockResolvedValue(1) };
  const holds = { create: jest.fn() };
  let svc: BdsLaunchService;

  beforeEach(() => {
    jest.clearAllMocks();
    tenants.getMe.mockResolvedValue({ mode: 'developer' });
    svc = new BdsLaunchService(
      repo as never,
      tenants as never,
      projectOs as never,
      txs as never,
      holds as never,
    );
  });

  it('open draft → open + snapshot price', async () => {
    repo.getById.mockResolvedValue(launch());
    repo.getOpenByProject.mockResolvedValue(null);
    repo.setStatusIf.mockResolvedValue(launch({ status: 'open' }));
    const out = await svc.open('L1', 't1');
    expect(out.status).toBe('open');
    expect(repo.setStatusIf).toHaveBeenCalledWith(
      'L1',
      'open',
      expect.objectContaining({ opened_at: expect.any(Date), price_list_id: 3 }),
      'draft',
    );
  });

  it('open when another open on project → 409 launch_open', async () => {
    repo.getById.mockResolvedValue(launch());
    repo.getOpenByProject.mockResolvedValue(launch({ id: 'L2', status: 'open' }));
    await expect(svc.open('L1', 't1')).rejects.toMatchObject({ response: { error: 'launch_open' } });
  });

  it('open from open → 409 launch_status', async () => {
    repo.getById.mockResolvedValue(launch({ status: 'open' }));
    await expect(svc.open('L1', 't1')).rejects.toBeInstanceOf(ConflictException);
  });

  it('close open → closed and refund reservations', async () => {
    repo.getById.mockResolvedValue(launch({ status: 'open' }));
    repo.setStatusIf.mockResolvedValue(launch({ status: 'closed' }));
    await svc.close('L1', 't1');
    expect(txs.cancelLaunchReservations).toHaveBeenCalledWith(7, 't1');
  });

  it('broker list → 404', async () => {
    tenants.getMe.mockResolvedValue({ mode: 'broker' });
    await expect(svc.list('t1')).rejects.toBeInstanceOf(NotFoundException);
  });

  it('create without project_id → 400 project_id', async () => {
    await expect(svc.create({} as never, 't1')).rejects.toBeInstanceOf(BadRequestException);
  });
});
```

- [ ] **Step 2: Run — expect FAIL**

- [ ] **Step 3: Implement create / open / close / list / get**

`create`: `project_id` integer > 0. `hold_ttl_seconds` mặc định 180. `phase_id` optional; nếu có, `getPhase` phải cùng `project_id` không thì 404. Snapshot `price_list_id` từ phase nếu chưa gửi.

`open`: `canOpenLaunch`; `getOpenByProject` khác id → 409 `{ error: 'launch_open' }`; `setStatusIf(..., 'open', { opened_at, price_list_id }, 'draft')`. Unique race `23505` → 409 `launch_open`.

`close`: `canCloseLaunch`; `setStatusIf(..., 'closed', { closed_at }, 'open')`; rồi `txs?.cancelLaunchReservations(project_id, tenantId)` — lỗi log, **không** rollback close.

`cancelLaunchReservations` **không** sống trong launch service — thêm method trên `BdsTxService` ở Task 5. Task 4 mock method đó.

- [ ] **Step 4: Run create/open/close tests — PASS**

- [ ] **Step 5: Commit** — chỉ khi user yêu cầu.

---

### Task 5: BDS-37 trên TX + enqueue / promote / war-room

**Files:**
- Modify: `services/ptt-crm-api/src/bds/transactions/bds-tx.service.ts`
- Modify: `services/ptt-crm-api/src/bds/transactions/bds-tx.service.spec.ts`
- Modify: `services/ptt-crm-api/src/bds/transactions/bds-tx.repository.ts` — `listReservationByProject`
- Modify: `services/ptt-crm-api/src/bds/launches/bds-launch.service.ts`
- Modify: `services/ptt-crm-api/src/bds/launches/bds-launch.service.spec.ts`

**Interfaces:**
- Produces: `BdsTxService.cancelLaunchReservations(projectId, tenantId): Promise<number>`
- Produces: `enqueueOnConflict`, `promoteNext`, `warRoom`

- [ ] **Step 1: TX tests**

```ts
it('BDS-37 cancel reservation TXs on project', async () => {
  repo.listReservationByProject.mockResolvedValue([
    { id: 'tx1', stage: 'reservation', product_id: 9, tenant_id: 't1' },
  ]);
  inventory.getOrThrow.mockResolvedValue({ product_id: 9, status: 'reserved', row_version: 2 });
  repo.setStageIf.mockResolvedValue({ id: 'tx1', stage: 'cancelled' });
  const n = await svc.cancelLaunchReservations(7, 't1');
  expect(n).toBe(1);
  expect(repo.setStageIf).toHaveBeenCalledWith(
    'tx1',
    'cancelled',
    expect.objectContaining({ lost_reason: 'launch_window' }),
    'reservation',
  );
  expect(inventory.transition).toHaveBeenCalledWith(9, 'cancel', 2, 't1');
});
```

Dùng lại `unitEventForCancel('reserved')` = `cancel`.

`listReservationByProject`:

```sql
SELECT * FROM bds_transactions
WHERE project_id = $1 AND stage = 'reservation'
  AND ($2::uuid IS NULL OR tenant_id = $2)
```

- [ ] **Step 2: Launch enqueue / promote / war-room tests**

```ts
  it('enqueueOnConflict writes waiting row', async () => {
    repo.getOpenByProject.mockResolvedValue(launch({ status: 'open' }));
    repo.enqueue.mockResolvedValue({ id: 'q1', status: 'waiting' });
    await svc.enqueueOnConflict(7, { lead_id: 12, product_id: 9, tenant_id: 't1' });
    expect(repo.enqueue).toHaveBeenCalledWith(expect.objectContaining({ launch_id: 'L1', lead_id: 12 }));
  });

  it('enqueueOnConflict no-op when no open launch', async () => {
    repo.getOpenByProject.mockResolvedValue(null);
    await svc.enqueueOnConflict(7, { lead_id: 12, product_id: 9 });
    expect(repo.enqueue).not.toHaveBeenCalled();
  });

  it('promoteNext creates hold for oldest waiting', async () => {
    repo.getOpenByProject.mockResolvedValue(launch({ status: 'open' }));
    repo.peekWaiting.mockResolvedValue({
      id: 'q1', launch_id: 'L1', product_id: 9, lead_id: 12, channel_partner_id: '', status: 'waiting',
    });
    repo.setQueueStatusIf.mockResolvedValue({ id: 'q1', status: 'promoted' });
    inventory = undefined; // hold.create mock
    holds.create.mockResolvedValue({ id: 'h2' });
    await svc.promoteNext(7, 9, { tenantId: 't1', row_version: 4 });
    expect(holds.create).toHaveBeenCalledWith(
      9,
      expect.objectContaining({ lead_id: 12, row_version: 4 }),
      expect.objectContaining({ tenantId: 't1' }),
    );
  });

  it('promoteNext no queue → 0', async () => {
    repo.getOpenByProject.mockResolvedValue(launch({ status: 'open' }));
    repo.peekWaiting.mockResolvedValue(null);
    await expect(svc.promoteNext(7, 9, { tenantId: 't1', row_version: 1 })).resolves.toBeNull();
  });
```

`promoteNext`: peek → `setQueueStatusIf` promoted → `holds.create`. Nếu create 409: để queue `waiting` lại (`setQueueStatusIf` promoted miss / revert cancelled không — **khóa:** nếu create fail, `setQueueStatusIf('waiting')` lại nếu vừa promoted; đơn giản hơn: create **trước**, thành công mới promoted. Khóa: create trước, rồi promoted.

`warRoom`: broker 404; map holds + `ttlRemainingSec`; `conflicts` = `countWaitingByProduct`.

- [ ] **Step 3: Implement**

- [ ] **Step 4: Run**

`cd services/ptt-crm-api && ./node_modules/.bin/jest src/bds/launches/bds-launch.service.spec.ts src/bds/transactions/bds-tx.service.spec.ts --runInBand`

Expected: PASS.

- [ ] **Step 5: Commit** — chỉ khi user yêu cầu.

---

### Task 6: Hook hold (BDS-36) + controller + module

**Files:**
- Modify: `services/ptt-crm-api/src/bds/hold/bds-hold.service.ts`
- Modify: `services/ptt-crm-api/src/bds/hold/bds-hold.service.spec.ts`
- Create: `services/ptt-crm-api/src/bds/launches/bds-launch.controller.ts`
- Create: `services/ptt-crm-api/src/bds/launches/bds-launch.controller.spec.ts`
- Modify: `services/ptt-crm-api/src/bds/bds.module.ts`

**Interfaces:**
- Consumes: `isBdsLaunchEnabled()`, `BdsLaunchRepository.getOpenByProject`, `computeLaunchExpiresAt`, `BdsLaunchService.enqueueOnConflict` / `promoteNext`
- Hold inject: `@Optional() launchRepo?: BdsLaunchRepository`, `@Optional() launches?: BdsLaunchService`

**Tránh vòng Nest:** `BdsHoldService` chỉ inject **repository** để đọc TTL. `enqueueOnConflict` / `promoteNext` gọi qua `@Optional() launches`. `BdsLaunchService` inject `BdsHoldService` cho promote. Nếu Nest circular: dùng `forwardRef(() => BdsHoldService)` trên launch service.

- [ ] **Step 1: Hold tests**

Trong `bds-hold.service.spec.ts` thêm mocks `launchRepo` / `launches`:

```ts
it('BDS-36: open launch → expires_at now+180s', async () => {
  process.env.PTT_BDS_LAUNCH = '1';
  launchRepo.getOpenByProject.mockResolvedValue({
    id: 'L1', status: 'open', hold_ttl_seconds: 180,
  });
  const now = new Date('2026-08-22T10:00:00Z');
  // existing create mocks for available unit...
  const out = await svc.create(9, { lead_id: 1, row_version: 1 }, { tenantId: 't1', now });
  expect(out.expires_at?.toISOString()).toBe('2026-08-22T10:03:00.000Z');
});

it('LAUNCH off ignores open launch TTL', async () => {
  process.env.PTT_BDS_LAUNCH = '0';
  launchRepo.getOpenByProject.mockResolvedValue({ status: 'open', hold_ttl_seconds: 180 });
  // expect expires_at ~ now+30m (P2 default planning)
});

it('409 during open launch enqueues', async () => {
  process.env.PTT_BDS_LAUNCH = '1';
  launchRepo.getOpenByProject.mockResolvedValue({ id: 'L1', status: 'open', hold_ttl_seconds: 180 });
  inventory.getOrThrow.mockResolvedValue({ product_id: 9, project_id: 7, status: 'hold', tenant_id: 't1' });
  await expect(svc.create(9, { lead_id: 2, row_version: 1 }, { tenantId: 't1' })).rejects.toBeInstanceOf(
    ConflictException,
  );
  expect(launches.enqueueOnConflict).toHaveBeenCalledWith(
    7,
    expect.objectContaining({ product_id: 9, lead_id: 2 }),
  );
});
```

`expireDue` sau mỗi hold expired thành công:

```ts
if (isBdsLaunchEnabled()) {
  try {
    const unitAfter = await this.inventory.getOrThrow(hold.product_id);
    await this.launches?.promoteNext(hold.project_id, hold.product_id, {
      tenantId: hold.tenant_id ?? undefined,
      row_version: Number(unitAfter.row_version),
    });
  } catch (err) {
    this.logger.warn(`promoteNext ${hold.product_id}: ${String(err)}`);
  }
}
```

Trong `create`, khi `status === 'available'` fail vì không available:

```ts
if (String(unit.status) !== 'available') {
  if (isBdsLaunchEnabled()) {
    await this.launches?.enqueueOnConflict(Number(unit.project_id), {
      product_id: productId,
      lead_id: body.lead_id,
      tenant_id: opts.tenantId ?? (unit.tenant_id != null ? String(unit.tenant_id) : null),
      channel_partner_id: body.channel_partner_id,
      requested_by_staff_id: body.requested_by_staff_id ?? null,
    });
  }
  throw new ConflictException({ error: 'unit_locked' });
}
```

TTL block:

```ts
let expiresAt: Date | null = null;
if (status === 'active') {
  if (isBdsLaunchEnabled()) {
    const open = await this.launchRepo?.getOpenByProject(Number(unit.project_id));
    if (open) {
      expiresAt = computeLaunchExpiresAt(now, open.hold_ttl_seconds);
    }
  }
  if (!expiresAt) {
    expiresAt = computeExpiresAt(now, ttlMinutes(ctx.status, tenantTtl));
  }
}
```

- [ ] **Step 2: Controller**

| Method | Path | Body | Lỗi |
|--------|------|------|-----|
| POST | `/projects/:id/launches` | `{ phase_id?, hold_ttl_seconds?, starts_at?, ends_at? }` | 400 `project_id` |
| GET | `/launches?project_id=` | | 404 broker |
| GET | `/launches/:id` | | 404 |
| POST | `/launches/:id/open` | | 409 `launch_open` / `launch_status` |
| POST | `/launches/:id/close` | | 409 `launch_status` |
| GET | `/launches/:id/war-room` | | 404 broker |

Guards: `StaffOrInternalKeyGuard`, `BdsPackGuard`, `BdsLaunchGuard`.

Controller spec: `open` delegate id + tenant.

- [ ] **Step 3: Register `bds.module.ts`**

Import guard, repo, service, controller. Providers + export `BdsLaunchService`.

- [ ] **Step 4: Run**

```bash
cd services/ptt-crm-api && ./node_modules/.bin/jest src/bds/launches src/bds/hold src/bds/guards/bds-launch.guard.spec.ts src/bds/bds.flags.spec.ts --runInBand
```

Expected: PASS (BDS-36 + P2 không regress).

- [ ] **Step 5: Commit** — chỉ khi user yêu cầu.

---

### Task 7: UI mỏng + nav «Ra quân» + roadmap

**Files:**
- Modify: `services/ops-web/src/lib/bds/nav.ts`
- Modify: `services/ops-web/src/lib/bds/nav.spec.ts`
- Modify: `services/ops-web/src/lib/bds/api.ts`
- Modify: `services/ops-web/src/components/layout/nav-icons.tsx`
- Create: `services/ops-web/src/app/crm/bds/launches/page.tsx`
- Modify: `docs/superpowers/plans/2026-08-22-bds-coding-roadmap.md`

- [ ] **Step 1: Nav tests**

```ts
it('CĐT with launches cap shows Ra quân', () => {
  process.env.NEXT_PUBLIC_PTT_BDS_UI = '1';
  const links =
    buildBdsNavSections(
      user([
        { section: 'bds_tenant', action: 'view' },
        { section: 'bds_launches', action: 'view' },
      ]),
      'developer',
    )[0]?.links ?? [];
  expect(links.some((l) => l.href === '/crm/bds/launches' && l.label === 'Ra quân')).toBe(true);
});

it('broker never shows Ra quân', () => {
  process.env.NEXT_PUBLIC_PTT_BDS_UI = '1';
  const links =
    buildBdsNavSections(
      user([
        { section: 'bds_baskets', action: 'view' },
        { section: 'bds_launches', action: 'view' },
      ]),
      'broker',
    )[0]?.links ?? [];
  expect(links.some((l) => l.href === '/crm/bds/launches')).toBe(false);
});
```

`buildDeveloperLinks` — sau Hold / trước Giao dịch:

```ts
if (hasCap(user, 'bds_launches', 'view')) {
  links.push({ href: '/crm/bds/launches', label: 'Ra quân' });
}
```

`nav-icons.tsx`: `'/crm/bds/launches': 'rocket'`.

- [ ] **Step 2: API helpers**

```ts
export type LaunchRow = {
  id: string;
  project_id: number;
  phase_id: string | null;
  hold_ttl_seconds: number;
  price_list_id: number | null;
  status: 'draft' | 'open' | 'closed';
  opened_at: string | null;
  closed_at: string | null;
};

export async function fetchBdsLaunches(token: string, projectId?: number): Promise<LaunchRow[]> {
  const qs = projectId != null ? `?project_id=${projectId}` : '';
  return bdsFetch(token, `/api/v1/bds/launches${qs}`);
}

export async function fetchBdsWarRoom(token: string, launchId: string): Promise<{
  launch: LaunchRow;
  holds: Array<{ hold_id: string; product_id: number; ttl_remaining_sec: number | null; status: string }>;
  queues: Array<{ id: string; product_id: number; lead_id: number; status: string }>;
  conflicts: Array<{ product_id: number; waiting: number }>;
}> {
  return bdsFetch(token, `/api/v1/bds/launches/${launchId}/war-room`);
}

export function postOpenLaunch(token: string, id: string) {
  return bdsMutate(token, `/api/v1/bds/launches/${id}/open`, 'POST', {});
}
export function postCloseLaunch(token: string, id: string) {
  return bdsMutate(token, `/api/v1/bds/launches/${id}/close`, 'POST', {});
}
```

Tái dùng `bdsMutate` đã có từ P9 trong `api.ts`. Nếu tách function chưa export — export hoặc copy block P9.

- [ ] **Step 3: Page** `launches/page.tsx`

`useBdsPageAuth([{ section: 'bds_launches', action: 'view' }])`. List launch. Chọn hàng:

- `draft` + cap `bds_launches` **open** → nút **Mở ra quân**
- `open` + cap open → nút **Đóng**; 3 cột: hold (TTL giây) · queue FIFO · xung đột (`waiting`)
- `useEffect` poll `fetchBdsWarRoom` mỗi 3000ms khi selected `open` (clear interval on unmount / đổi hàng)
- LAUNCH=0 / 404 → «Ra quân chưa bật»

Copy: Giữ chỗ · hàng đợi · xung đột. Không chat huddle.

- [ ] **Step 4: Roadmap**

Bảng pha: P10 → `[bds-p10-launch.md](./2026-08-22-bds-p10-launch.md)` · BDS-36.  
§3 P10: `bds_launches` / queue / TTL 180s / BDS-37 / flag `PTT_BDS_LAUNCH`.  
§4 hàng 14: `PTT_BDS_LAUNCH | mặc định 0; ra quân + TTL 180s. Bật khi PACK=1 + P2 (+ P3 khuyến nghị khóa giá).`

- [ ] **Step 5: Verify**

```bash
cd services/ptt-crm-api && ./node_modules/.bin/jest src/bds --runInBand
cd services/ops-web && ./node_modules/.bin/vitest run src/lib/bds
cd services/ptt-crm-api && npm run build
cd services/ops-web && npm run build
```

Expected: Jest xanh; Vitest xanh; 2 build exit 0.

- [ ] **Step 6: Commit** — chỉ khi user yêu cầu.

---

## 4. Definition of Done

- [ ] BDS-36: launch `open` + POST hold inhouse → `expires_at` = now+180s (±1s)
- [ ] LAUNCH=0: cùng hold → TTL P2 (phút), không enqueue
- [ ] UC-045: open khóa `price_list_id`; close → hold mới dùng TTL CSBH
- [ ] UC-046: 201 + 409 `unit_locked`; 409 có row `bds_unit_queues.waiting`; expire → promote hold mới
- [ ] BDS-37: close → TX `reservation` `cancelled` + `lost_reason=launch_window` + căn `available`
- [ ] Một project hai `open` → 409 `launch_open`
- [ ] Broker GET `/launches` → 404
- [ ] Flag off → route launch **404**
- [ ] Nav CĐT «Ra quân»; sàn không có
- [ ] War-room poll, không SSE
- [ ] Prod không bật `PTT_BDS_LAUNCH`

---

## 5. Rollback

`PTT_BDS_LAUNCH=0` (+ PACK=0 nếu cần). Route 404. Hold P2 nguyên. Không DROP bảng.

---

## 6. Deploy VPS (khi user yêu cầu)

1. `bash scripts/apply_pg_ddl_bds_p10.sh` trên VPS.
2. rsync `ptt-crm-api/dist/` + `ops-web/.next/standalone/`.
3. `systemctl restart realosai-api realosai-ops-web`.
4. **Không** ghi `.env` `PTT_BDS_LAUNCH=1` trên prod.

Staging:

```bash
PTT_BDS_PACK=1
PTT_BDS_HOLD_TTL=1
PTT_BDS_LAUNCH=1
PTT_BDS_UI=1
NEXT_PUBLIC_PTT_BDS_UI=1
```

---

## 7. Sau P10 xanh

**P11** staff chat + huddle `launch_*`.  
**P12** ticket việc.  
**P10b (optional):** checklist pre-open, SSE war-room, snapshot giá từng căn.

---

*P10 thắng: mở ra quân thì hold mới chết sau 180s; 409 vào hàng đợi; đóng cửa sổ nhả giữ chỗ chưa cọc; CĐT thấy «Ra quân».*
