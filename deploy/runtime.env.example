# Deploy-owned runtime overrides (WIN-4-A). Generated by scripts/vps_enable_win4a_keycloak.sh
# Do not commit secrets — this file is gitignored.
#
# Zero SQLite (Wave 3–4): production must NOT set PTT_SQLITE_PATH — Nest ignores it.
# Use deploy/env.zero-sqlite-w3-prod.example as the flag matrix; run ./scripts/ci_zero_sqlite_w3_gate.sh pre-deploy.
#
# MKT-AI planner (see docs/runbooks/mkt-ai-planner-delivery-sop.md)
# PTT_MKT_AI_PLANNER_ENABLED=1
# PTT_MKT_AI_PLANNER_SLUGS=meta-lead-gen
